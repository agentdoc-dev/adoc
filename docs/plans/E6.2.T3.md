# E6.2.T3 — Explicit-audience redacted rendering

Status: accepted for execution under the user's continuing E6 scope. Root owns the single durable run; Claude reviews remain waived.

## Authority and dependency evidence

Reuse ROADMAP-V10 → v10 README hierarchy → EXECUTION-MAP E6.2 → MILESTONES T3, AUTHORIZATION A4, PRD v1.1 §7 and PRD v1.0 §47.8. Original V10.6.5 supplies nonconflicting rendering detail. T2 is complete on Adoc ab464c7d1d43ae05f93bb60ece07ca12bbff488e (PR221) and Cloud 1c6d592a01c69ecb3fd22415386cbb3fb37a2df6 (PR88); Cloud CI34105752699 passed. Both remote heads and repositories verified before creating isolated .worktrees/e6.2-t3, branch feat/e6.2-t3-rendering, stacked on feat/e6.2-t2-partial-fields.

Actual implementation is Adoc's HTML renderer, despite the umbrella E6.2 repo caption. Cloud only pins the paired runtime and retains workflow/context evidence. No new service, schema, database or managed authorization path. Internal and restricted are sensitive; absence/public is nonsensitive. Existing RetrievalPolicy is explicit local authority, its allowed_visibilities narrows the audience ceiling, and excluded_object_ids excludes existence. No new domain vocabulary or approval decision is needed.

## Contract and acceptance

1. First failing three-disposition fixture: default public rendering with a sensitive object emits a stable marker containing only kind and Object ID; explicitly authorized audience renders content; existence-excluding policy omits the object and any carrier that would expose its ID. Marker returns before status, body, evidence or metadata rendering. Repeat inputs produce identical bytes. Use one authored CSS constant following quarantined-html precedent.
2. Preserve authorized sibling fields: body-sensitive content is withheld; optional generic metadata is omitted. Dedicated metadata must never escape through badges, CSS modifiers or specialized renderers. Reuse compiled fields and metadata projection. If withholding a required typed discriminant cannot yield a truthful full card, use the restricted object marker instead of fabricating status. Field visibility never promotes a hidden field into whole-object sensitivity when a safe sibling projection exists.
3. Existence-exclusion wins for every audience. Apply conservative carrier closure to references, relations, evidence, resolved_by, contradiction claims, nested prose/list/table/link/footnote/code text as needed; use the existing retrieval fixed-point precedent. Do not merely hide anchors while leaking plain text IDs. Ordinary references to marker-visible restricted objects may retain kind/ID existence. Do not infer content authority from those references.
4. Derive lifecycle badges only from authorized surviving contributions; hidden contradictions/status/claims/expiry fields must not change visible output. Canonical AST and graph/hash bytes remain unchanged. A rendering projection never writes governed state.
5. Thread explicit policy through the sole production sink, compile.rs::build_artifacts_for_build → HtmlRenderer. Proposed additive internal entry: render_workspace_for_date_and_policy(workspace, today, policy: Option<&RetrievalPolicy>) -> String. Existing compile-only entry uses default public redaction. Validate explicit policy at core build boundary before outputs/provider calls; existing typed retrieval audience/policy errors are reused.
6. CLI adoc build --audience public|internal|restricted selects an explicit local audience. Existing project retrieval_policy supplies configured audience and restrictions; a flag changes only audience and retains configured exclusions/allowed_visibilities. With a flag but no config, canonical visibility set supplies allowed classes bounded by that explicit audience. A trusted LocalContext gateway policy override takes precedence over both config and caller audience. MCP build arguments do not gain an authority field. No environment audience source. Unknown flag/config audience refuses with typed diagnostic and no output.
7. Existing no-visibility corpus HTML remains byte-identical. Graph/search artifact bytes remain unchanged in this slice. Search/vector exclusion is explicitly E6.3.T3 under current authority; retain the shared fixture for its later cross-slice assertion. Do not claim the E6.2 milestone's complete HTML/search-vector/authorized property from rendering tests alone.

## Current code and ownership

Verified interfaces: local BuildInput currently has path/out/no_embeddings/as_of; core BuildInput has root/embeddings/prior_search_artifact_path. Local build_with_context loads config but discards retrieval_policy. HtmlRenderer builds contradicted_claim_ids from all pages before render_block. Metadata and dedicated body/evidence/header helpers all converge in html.rs. BuildOptions and core public build wrappers must carry the resolved policy.

Renderer worker owns crates/adoc-core/src/infrastructure/render/html.rs and, only if needed, a small sibling render helper; begins with the failing three-disposition fixture and owns renderer regressions. Plumbing worker owns core application/compile.rs and lib.rs, local build input/context resolution, CLI build flag/dispatch, MCP literal compatibility, and their integration tests. Both use the agreed renderer method and are not alone: preserve others' edits. Root owns Cloud paired pin, plan/context, cross-checks and all state. No recursive delegation.

## Checks and review

Adoc: cargo fmt --all -- --check; cargo test --workspace --locked; cargo clippy --workspace --all-targets --locked -- -D warnings; cargo build --workspace --locked; cargo doc --workspace --no-deps --locked; staged prek; retained runtime golden parity/determinism/tamper refusal. Focused red/green tests must include default/authorized/existence-excluded rendering, generic/body/dedicated fields, hidden contributor status, nested references, CLI/config/trusted-policy precedence, unknown audience and unchanged pilot goldens.

Cloud companion required commands: pnpm lint; pnpm format:check; pnpm typecheck; pnpm test; pnpm build; bash scripts/domain-authority-scan.sh; bash scripts/contract-scan.sh; bash scripts/ui-roadmap-check.sh. No SQL or route behavior changes planned; existing native checks are retained from T2, and exact remote CI still runs full suite. Use the exact retained final Adoc binary for runtime checks. Avoid simultaneous shared DB users and reset before full DB-bearing unit runs.

Independent Codex specialist review on frozen final commits; stable E62T3 finding IDs and dispositions; two repairs/four attempts, exhaustion means diagnosis/escalation. Fix valid findings, scoped rereview, final required checks and evidence before done/PR. Commit before final review to keep fingerprints stable. Rollback is source-only; retain prior sensitive managed capability gates. No merge, deploy or release. Then select next dependency-ready E6 slice.
