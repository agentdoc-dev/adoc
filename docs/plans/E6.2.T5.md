# E6.2.T5 — No-visibility compatibility cut

Accepted for execution under the continuing E6 instruction. Root coordinates run state, findings and PRs. Claude is waived; independent Codex review remains required (two repairs/four reviews).

## Authority and existing state

ROADMAP-V10 → current v10 README hierarchy → EXECUTION-MAP E6.2 → MILESTONES T5. Reuse accepted PRD/ADRs/domain; aw-domain identifies no new terminology or invariant gap. T4 is reviewed and locally verified at Adoc17ea32f3034809e8a87435bd39b05156de68f817 / Clouddf09381cd54f7cf3611386d4d5f773be8427c50d, PR223/90 open. T4 hosted CI34115402505 succeeded on the exact reviewed Cloud head. Fresh .worktrees/e6.2-t5 branches feat/e6.2-t5-compatibility were chosen only after verifying those remote base heads and repository identities. Reference worktrees are not instructions to develop there.

Accepted graph-v5→v6 migration changes schemas/hashes/source bindings. The compatibility comparator is exact pre-E6 graph-v6/search-v2 implementation e1b74a3f579933e3f8194c6c998f91a24578e9db (unpublished package0.4.0), direct parent of first E6.1 commit. Retained executable SHA2569b4ad1331514d79f5c772ad3ba2ad73cbcd2c25bf828afe8fa2f6b71b6476d6f is bound in prior committed pilot evidence. Latest published v0.3.4 uses graphv5; never claim cross-schema byte identity or remove fields to conceal differences. Baseline rebuild instructions and exact external executable are retained in E6.2.T5-baseline.json and Adoc preflight plan.

## Minimal implementation and acceptance

1. Adoc worker owns tests/fixtures and docs only: new crates/adoc-cli/tests/e6_2_compatibility.rs; three baseline-produced golden artifacts and baseline.json under fixtures/e6_2_compatibility; one additional unclassified actual stdio test in crates/adoc-mcp/tests/stdio_dogfood.rs; docs/pilots/e6-2-t5-compatibility.md. Reuse v0_6/project fixture, TestWorkspace/copy_tree/fixture_path/adoc_command, StdioServer and structured_content. No production changes without a reproduced mismatch and coordinator adjudication.
2. Independently run the exact baseline and candidate in the same temporary source workspace, same docs path, fixed --as-of2026-09-07, deterministic embedding provider, and separate empty output directories. Clear ambient configuration/audience/retrieval-policy inputs. Generate goldens only from baseline. Pin binary/commit/source hashes/date/provider/arguments/artifact hashes in the manifest; verify those bindings explicitly. Compare raw docs.html, docs.graph.json and docs.search.json bytes, including final newline. Parse separately only to assert schemas and absence of visibility/field_visibility/provenance/declassification opt-ins. Rebuild candidate once for determinism. No timing matrix.

```rust
for artifact in ["docs.html", "docs.graph.json", "docs.search.json"] {
    assert_eq!(fs::read(baseline.join(artifact))?, fs::read(candidate.join(artifact))?);
    assert_eq!(fs::read(baseline.join(artifact))?, fs::read(golden.join(artifact))?);
}
```

3. Normal workspace test compares candidate artifacts to immutable baseline goldens. An explicit external-binary gate runs both producers independently and is mandatory for T5; its ignored-by-default prerequisite must not be counted as passing without execution. Verify the harness detects an isolated changed artifact byte/source binding. Compare clean lexical search/why stdout/stderr/status on independently built artifacts without normalization. Preserve existing richer CLI gates.
4. Actual stdio search+why compares the complete unclassified LocalContext outcome to structured content and exact text JSON bytes, with nonempty records and absent optional classification/managed metadata. Reuse a date-free ordinary fixture and existing no_embeddings lexical setup. WhyOutcome becomes RetrievalEnvelope over KnowledgeObject records; search goes through serde_json bytes to preserve f32 wire serialization. JSON-RPC framing is separate from the retrieval payload. Ambient hints do not establish authority.
5. Reuse existing unknown-audience typed-refusal tests for build/retrieval/gateway startup, existing HTML goldens and managed no-opt-in forwarding. No new policy engine. E6.3 audit/spool/embedding privacy remains unfinished and outside T5.

## Cloud and ownership

Cloud runtime search and genuine HTTP why already compare full ordinary response bytes to direct CLI, and private contributors retain legacy shape. Inspected tests and native code are byte-identical to final T4. No production/native/test strengthening is indicated. Root owns paired registry pin plus compatibility assertion, this plan and knowledge relationships, final Cloud checks, all DB operations and state. Worker is not alone, preserves others' edits and does not delegate or commit. Existing native security review and 2,671 pgTAP proof carry only through explicit exact-source/mode equivalence; runtime-dependent checks use the final candidate.

## Required checks and completion

Adoc: new normal compatibility test and explicitly invoked independently_built_pre_e6_artifacts_are_byte_identical with retained baseline; actual stdio tests; build_audience_cli/retrieval_policy_errors_cli/check_cli/billing_pilot; staged prek (fmt/clippy/workspace tests/hygiene), cargo build --workspace --locked, cargo doc --workspace --no-deps --locked, git diff --check. Record actual counts/skips, raw producer/source hashes and final runtime digest. Existing optional performance gates remain explicitly ignored, not newly passed.

Cloud logical required checks: pnpm lint; pnpm format:check; pnpm typecheck; pnpm test; supabase db reset; pnpm test:db; pnpm build; pnpm test:e2e; bash scripts/domain-authority-scan.sh; bash scripts/contract-scan.sh; bash scripts/ui-roadmap-check.sh. Reuse unchanged native evidence via exact git/source/config proof; rerun runtime-dependent checks when paired pin advances. Use isolated cloud_e6 DB and T5-specific external app config on3116; no shared U1 database resets. No test skipping disguised as acceptance.

Commit final source before independent Codex review focused on baseline provenance/independent production/raw comparisons/stdio payload and carried security boundaries. Preserve finding IDs and scoped rereviews; limits exhaust to diagnosis. Checkpoint evidence and next action, create paired PRs on verified T4 bases and join CI. No merge/deploy. Test-only rollback removes new proof files; never rewrite historical goldens from candidate to hide mismatches. No material product decision remains unresolved.
