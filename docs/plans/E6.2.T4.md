# E6.2.T4 — Governed selected-field declassification

Accepted for execution under the user's continuing E6 instruction. Coordinator alone owns run state. Claude review is waived; independent Codex specialist review remains required.

## Authority, dependencies and decision

ROADMAP-V10 → current v10 README hierarchy → EXECUTION-MAP E6.2 → MILESTONES E6.2.T4, AUTHORIZATION A3/A4, K7 and accepted PRD/ADRs govern this slice. T3 is complete at Adoc 960c803f274a053bb943a152dcb328112125b91e (PR222) and Cloud f95d709a4a78cfcd96adb0a04a8979eb9f9c7b66 (PR89), hosted CI34109506857 succeeded. Remote heads and repositories were verified before choosing isolated .worktrees/e6.2-t4, branch feat/e6.2-t4-declassification, based on those T3 heads. Main and reference document worktrees are not development bases.

User decision, 2026-09-07, “Yes allow”: an authorized exact-version selected-field declassification may override object, authored-field and contributing classification floors for those selected fields. All other fields remain restricted; current source ACL and AgentDoc permission remain ceilings. No model-created authority.

Bounded implementation rules applying A4: selected fields require the existing sealed complete T1 manifest; missing provenance cannot be treated as source-free. Keep /body and /fields/<one RFC6901 token> selectors. Only derived fields lower; restricted_evidence_remains_hidden must be true, and no source assertion/snapshot is rewritten or released. Source-free enrollment, dedicated-member selectors, source disclosure and correction/revocation commands are outside this slice. Bare historical declassification/correction markers remain inert without an admitted detail. These are explicit implementation limits, not additional user decisions.

## Contract and native path

Add immutable adoc.managed_field_declassification.v0 using existing domain validation/schema registration patterns. Closed fields: schema_version, request_id, workspace_id, canonical_id, version_id, content_digest, provenance_record_digest, state_event_ordinal, state_event_digest, fields, principal_id, auth_session_id, authorization_decision_id, policy_version, rationale, effective_date, restricted_evidence_remains_hidden. Each field row contains selector, prior_classification, new_classification, assertion_ids. Existing UUID/digest/date/100-field/100-assertion bounds and duplicate rejection apply. Rationale is 1..4096 Unicode scalars and nonblank; event ordinal is zero-based 0..9007199254740991; policy_version is exact stored nonblank text, not a UUID. Reference validation requires strict descent but does not compare successor prior to the original object floor: native admission proves the serialized chain. Shape never grants authority.

```sql
public.record_managed_field_declassification(
  p_workspace_id uuid, p_request_id uuid, p_request_bytes text,
  p_authorization_decision_id uuid
) returns jsonb -- exact record_bytes and record_digest
```

Authenticated human only. Derive actual principal/session; require current knowledge.declassify and an exact native object-scope allowed authorization decision, copying its policy_version. Bind exact active version/content, sealed manifest digest and complete selected assertion sets; current baseline and every contributing source must remain readable. Strict lowering is checked natively. Model/service/workload requests cannot authorize. Existing advisory proposal/escalation paths remain available without granting this permission.

One append-only native detail table retains exact request/record bytes and digest, indexed workspace/request/event coordinates and approver evidence, under forced RLS and no direct caller/service access. New RPC atomically appends the existing bare declassification event and its bound detail. Reuse event chain and transaction-bound one-use admission precedent from effectivity transitions. No change to historic event bytes/ordinals/digest chain. Do not expose rationale/assertion coordinates through broad event SELECT. Exact request-ID replay returns the original record only after current approval/session/source checks; mismatched bytes conflict.

Multiple approvals form a strictly descending per-selector chain: prior class equals the latest admitted target, initially the strictest contributing floor. Effective dates must be nondecreasing per selector. Three classes bound the chain to at most two lowerings. Serialize admission and reject stale prior/out-of-order requests; latest currently effective approval applies. UTC date comes from the final native clock. Approval cannot affect reads before the event existed. Exact immutable history proves the chain; no new generalized scheduling engine or replacement event is needed.

Lock order preserves T1/T2: provenance/classification locks, declassification table lock and serialized approval key, event/FK locks and workspace event chain before final clock, then existing canonical authority/source try-lock paths. Reevaluate session/permission/source and exact joins after persistence; expiry rolls back event and detail together. Read/prepare/finalize holds declassification SHARE before policy/authority locks. Full projection equality, including approval reference, is rechecked on every contributor, both passes, replay and after audit insertion. Preserve READ COMMITTED and current-clock checks. Existing manifest enrollment keeps raw/legacy whole-object RLS refusal.

Private effective projection row:

```json
{"selector":"/body","classification":"public","declassification":{"state_event_ordinal":0,"state_event_digest":"sha256:...","detail_digest":"sha256:...","prior_classification":"restricted"}}
```

No reference retains T1 floor algebra. Null remains unresolved and never lowers. Valid reference plus fresh native authority applies effective class directly to its selected field. Native direct-access algebra and shared core must agree. Cloud transports this closed shape and always requires --require-field-declassification, so an older runtime refuses.

## Core projection and acceptance

Reuse field_projection validation/project_object and existing reference fixed point. Validate original receipts and graph first. When an original object floor exceeds audience, retain only explicitly approved visible scalar fields and identity/hash binding; suppress every unselected generic/dedicated field, source location, lifecycle, outgoing relationship, evidence and derived carrier. Serialize withheld source as {} using an explicit internal state admitted only for assembly-produced projected IDs; ordinary malformed graph/source inputs still fail. Do not fabricate a status or location. Ensure graph indexing, contradiction refresh, why enrichment and CLI formatters cannot restore hidden metadata. Existence exclusions always win. When the object is ordinarily readable, retain existing authorized sibling behavior. Canonical bytes/hashes and ordinary no-visibility output remain unchanged.

Acceptance checks:

1. First failing real fixture: restricted object/body/contributors, approved /body public exposes only body plus identity/hash, source {}, no owner/status/evidence/lifecycle/reference leakage. Search and why agree, including text formatters. Ordinary and authorized sensitive sibling behavior remains correct and audited.
2. Immutable detail round-trip/schema and native event linkage prove exact version/fields/prior/new/assertions/principal/session/decision/policy/rationale/date/posture. Bare marker, forged reference and model proposed lowering grant nothing; advisory escalation remains possible.
3. Reject foreign/stale/insufficient approval, service/workload, missing or unknown provenance, bad UUID/digest/selector/duplicate shape, nonlowering, wrong assertion set, request conflicts, event mismatch. Current source revocation still hides approved fields. Do not relabel retained snapshots.
4. Replay, disjoint/repeated lowering and future activation are deterministic. New effective detail invalidates prepared input. Admission/finalization lock contention, policy/source ABA, expiry after persistence/audit, replay and raw RLS fail closed without partial events/audit commits.
5. Exercise genuine authenticated RPC and Cloud HTTP retrieval using the paired retained core binary; transport malformed references refuses. Run unchanged compatibility/golden parity and tampered runtime-pin refusal.

## Ownership and implementation order

Adoc worker owns Adoc contract/domain/schema/registry and core projection, graph/retrieval/formatter/capability changes plus Rust tests/docs. Native worker owns only new Cloud migration and native pgTAP fixtures. Root owns Cloud runtime adapter/transport tests, HTTP E2E, paired pin, domain/context/plan, all DB execution and workflow checkpoints. Workers are not alone; preserve others' edits, coordinate DTOs directly, no nested delegation. Native worker must provide fixture setup seam for root HTTP tests. Begin with failing acceptance checks, then implementation. External core/native proposal files retain detailed inspected seams; this plan controls conflicts.

## Required checks, review and rollback

Cloud exact required commands: pnpm lint; pnpm format:check; pnpm typecheck; pnpm test; supabase db reset; pnpm test:db; pnpm build; pnpm test:e2e; bash scripts/domain-authority-scan.sh; bash scripts/contract-scan.sh; bash scripts/ui-roadmap-check.sh. Root serializes DB users and resets before fixed-fixture full runs. Pin ADOC_REGISTRY_REF and retained runtime to the final paired Adoc commit.

Adoc: cargo fmt --all -- --check; cargo test --workspace --locked; cargo clippy --workspace --all-targets --locked -- -D warnings; cargo build --workspace --locked; cargo doc --workspace --no-deps --locked; staged prek; exact retained runtime parity/determinism and tamper refusal. Preserve red/green logs and source-byte equivalence when committing after tests.

Commit final source before independent specialist review of native authorization/concurrency and shared projection/metadata paths. Stable E62T4 finding IDs/dispositions; two repair rounds/four review attempts, exhaustion requires diagnosis/escalation. Resolve valid findings with scoped rereview; all final required checks must cover final source. Record evidence/next action, open paired stacked PRs and check exact-head CI. No merge/deploy. Migration is additive: disable new admission to stop new lowerings; do not delete immutable historical details or run an old incapable runtime against declassified projections. Continue dependency-ready E6 work after completion.
