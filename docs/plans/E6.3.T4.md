# E6.3.T4 — Revocation invalidates existing derived access material

Accepted under the continuing E6 instruction; T3 hosted CI34133062095 passed before this execution. Fresh paired `.worktrees/e6.3-t4` branches `feat/e6.3-t4-derived-revocation` were prepared from verified published Adoc e57fd17cf93a0f9e073cb7fb54f0039ca8d26c3d (PR227) and Cloud d61af1018ffb0ea90b157a629e6ef85ed3e2d58a (PR97), base branch `feat/e6.3-t3-embedding-exclusion`. Live heads/open PRs and absence of prior T4 branches/worktrees were checked before creation. Reference worktrees were not development instructions.

Root owns the durable run, artifact relationships, findings, checks, commits and PRs. Independent Codex specialist review required; Claude waived. Two review repairs/four review attempts; exhaustion means diagnosis. Accepted PRD/domain/ADR and current ROADMAP-V10 → v10 README → EXECUTION-MAP → MILESTONES remain authority. Aw-domain found no unresolved gap: current Cloud authority, immutable history and ADR0065 process-lifetime operator policy apply. Aw-context connects this slice to existing E2.6 invalidation and derived-material admission, not a new permission engine.

Actual preflight found no confirmed production defect or persistent Cloud vector cache. Execute the missing cross-lifecycle proof first against the completed T3 implementation. If it already passes, complete a tests-and-documentation slice honestly. A failed fixture must be diagnosed before editing production; no artificial red, blanket cache deletion, live gateway hot reload, model download or speculative cache subsystem.

Adoc lifecycle worker owns only gateway_parity.rs and stdio_dogfood.rs tests. Root owns Cloud existing managed-retrieval E2E/lifecycle integration and documentation. If actual evidence shows a shared admission defect, root assigns its narrow repair. Workers are not alone, preserve all other changes, and never commit/push or modify run state. Root owns the sole isolated database; one worker owns Cargo at a time.

## Acceptance and architecture

Authority: MILESTONES E6.3.T4 line741 extends E2.6.T4 line330: revoke permission and prior derived access material cannot authorize a stale read. ADR0065 explicitly holds gateway operator policy for process lifetime; changing it requires restart. Keep this accepted boundary. Authored graph classification is independently reloaded for each application call. E6.3.T4-preflight.md found no persistent local response/session cache; the persisted Search Artifact is the existing derived material to exercise.

Plan a tests-and-authority-documentation slice unless a concrete lifecycle assertion fails. T3 already proves filtering before inference and broad-cache→narrow **rebuild**. T4 instead retains the actual old Search Artifact untouched across revocation and proves current read admission rejects its obsolete authority/composition. Do not add a generation table, cache registry, watcher, artifact schema/policy hash, background invalidator or audit-spool deletion. Previously exported file bytes are not retroactively erased by read admission.

## Two existing owned test files

One Adoc lifecycle worker can own these together; Cloud lifecycle/native authority is independently owned. Root owns ADR/plan/domain/pin/state and final integration. No production changes pre-authorized by this proposal.

1. `crates/adoc-cli/tests/gateway_parity.rs`: add **one** actual CLI permission-only revocation lifecycle test. Reuse `TestWorkspace`, deterministic CONFIG, `write_policy`, `build`, `artifact`, `cli`, `assert_bytes`. T3 adds the build/artifact helpers; verify their final names on the accepted T4 base. Do not modify its two T3 embedding producer tests or classified setup fixtures.
2. `crates/adoc-mcp/tests/stdio_dogfood.rs`: add **two** actual child-process lifecycle tests using existing `StdioServer::{spawn_with_config,send,receive}`, `structured_content`, and tempfile. The existing `Drop` kills and waits for the child, so explicit `drop(server)` supplies the real restart boundary. Reuse `support::CONFIG_YAML` (deterministic provider, docs path and dist output) for fixture setup. Do not pull in gateway_audit's HTTP fixture; public-before-revocation subjects and public projected-after-revocation subjects need no auditor, which keeps the proof about index admission rather than audit refusal.

A tiny local fixture/setup function in stdio_dogfood is reasonable only if shared by both new tests. Use ordinary source compilation for both graph versions; no handmade vector, graph hash, schema or synthetic projection. Existing initialization JSON at lines704 and815 supplies the exact protocol shape. Reuse the existing direct `AgentDocMcpServer::run_build(BuildParams)` setup pattern at line682, with deterministic config and embeddings enabled for the initial real artifact. Building fixtures in-process is existing practice; the retrieval lifecycle itself must run through the real stdio child.

## Compact fixture and important assertions

Use two unrelated public claims (no prose/references to cause unrelated missing-vector diagnostics). The first is the revocable target and the second the unchanged safe control. For the field test, include `field_visibility: owner=public, body=public` initially and replace that **same line** with `owner=restricted, body=restricted` later. Keeping line count stable prevents an unrelated source-location change in the safe control.

Illustrative source, valid existing authored syntax:

```adoc
# Lifecycle @doc(lifecycle.page)

::claim lifecycle.target
status: draft
owner: TARGET_OWNER_CANARY
field_visibility: owner=public, body=public
--
TARGET_BODY_CANARY remains readable before revocation.
::

::claim lifecycle.safe
status: draft
owner: public-team
--
SAFE_BODY_CANARY remains readable across the lifecycle.
::
```

The object-policy-only test can omit field_visibility entirely. Both objects being public lets an authorized positive response return through MCP without a recorder. This is a real authority change: public class does not override an explicit object exclusion.

Use `--objects-only --top 20` / MCP `objects_only:true, top:20` to ensure the two actual vectors are observed independently of similarity ordering. Capture the real initial Search Artifact bytes and entries; assert target **and** safe have nonempty vectors and the positive semantic response contains both with numeric vector_rank. Capture full source and graph bytes, target content_hash, and header before mutation. Re-check file bytes after every lifecycle phase. Do not equate a no-auditor sensitive refusal with a successful authorized read.

### 1. CLI object permission revocation without source/hash change

Proposed test: `cli_permission_revocation_rejects_retained_vectors_without_rebuilding`.

- Write public policy permitting both objects and build real deterministic embeddings once into dist. Preserve source bytes, `dist/docs.graph.json`, `dist/docs.search.json`, and initial target content_hash.
- Run semantic, hybrid and why positive controls using explicit graph/search paths. Search contains target and safe vectors; why returns target body. Capture safe-only why complete envelope for an ordinary byte control.
- Change **only** operator project config `excluded_object_ids` to `["lifecycle.target"]`; no source edit, no build, no vector rewrite. Each real CLI invocation rediscovers current config even with explicit artifact paths.
- Repeat semantic and hybrid with the retained old search path. Target ID/body/owner never appears; safe remains and has numeric vector_rank. Object-only revocation must not add `search.hash_drift` or artifact-missing warnings: the unchanged safe corpus has a valid vector, and hidden-only changes cannot signal hidden presence. Why target returns empty records, `retrieval.object_not_found`, exit3; search exit0. Whole target why envelope may mention its requested ID in a diagnostic, so require no returned records/canary rather than banning the caller-supplied ID from that request's entire envelope.
- Assert source, graph and Search Artifact bytes remain exactly equal to the captured originals and target graph content_hash is unchanged. Compare safe why via `assert_bytes` with its before envelope. Do not compare before/after full search ranks: removing a legitimate candidate can renumber the safe result.
- Optional cheap restoration control in the same test: clear exclusion, run semantic again against the **same** files, and compare the whole response to the original authorized response. No timing/date fields should be stripped.

Critical actual CLI shape (illustrative calls to existing helpers):

```rust
let old_search = fs::read(workspace.root.join("dist/docs.search.json")).unwrap();
let args = ["search", "lifecycle", "--semantic", "--objects-only", "--top", "20",
            "--artifact", "dist/docs.graph.json", "--search-artifact", "dist/docs.search.json"];
let allowed = cli(&workspace, &args, 0);
let mut revoked = initial_policy.clone();
revoked.excluded_object_ids.insert("lifecycle.target".into());
write_policy(&workspace, &revoked); // no build
let denied = cli(&workspace, &args, 0);
assert_eq!(fs::read(workspace.root.join("dist/docs.search.json")).unwrap(), old_search);
```

### 2. Authored field-classification change in the same MCP process

Proposed test: `stdio_reloads_field_revocation_without_reusing_old_composition_vectors`.

- Build once with public owner/body classifications and deterministic provider; retain actual Search Artifact bytes. Start one `StdioServer::spawn_with_config` with explicit public operator authority, initialize, and record `server.child.id()`.
- Send semantic and why positive requests. Target and safe have vector ranks; target why contains both canaries. This must be a successful public response with `isError:false`, not an audit refusal.
- Change only target field classification in source to restricted (same line count). Rebuild **graph only** at the same `dist/docs.graph.json` path with `no_embeddings:true` through existing `run_build` fixture seam or `adoc_build` request. Do not call an embedding-enabled build, regenerate the Search Artifact, or restart/rebind the server. Assert Search Artifact bytes remain equal, graph bytes and target content_hash change, and safe object's composition-related fields remain unchanged.
- Repeat requests with the same process ID, same graph path and same retained search path. Semantic excludes target because its old composition is invalid but still ranks safe. Hybrid searching `lifecycle.target` may return the permitted public target through lexical ID matching, with numeric lexical_rank and absent/null vector_rank. Neither search nor why may expose the restricted body/owner canaries; why retains target ID and its current canonical graph content_hash, projects body empty and omits owner per the existing serializer.
- The permitted target's stale composition legitimately yields fixed `search.hash_drift`; unlike pure excluded-object revocation, do not assert an empty diagnostics array here. Safe vectors keep semantic available (exit0), so blanket cache invalidation cannot satisfy the test.
- Keep `unclassified_stdio_search_and_why_preserve_complete_local_payloads` unchanged as the whole-wire ordinary compatibility control. A newly classified target's output is intentionally changed; compare only safe why complete payload before/after (stable line positions) if useful, rather than stripping projection/classification fields from target responses.

Actual MCP payload fields are already present in `SearchParams` at lib.rs816:

```json
{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{
  "name":"adoc_search","arguments":{"query":"lifecycle","semantic":true,
  "objects_only":true,"top":20,"artifact":"dist/docs.graph.json",
  "search_artifact":"dist/docs.search.json"}}}
```

Use `semantic:false` and omit lexical for hybrid. Use query `lifecycle.target` for the lexical partial control so hidden body text is not necessary to match it.

### 3. Operator policy revocation at the accepted MCP restart boundary

Proposed test: `stdio_restart_applies_operator_revocation_to_retained_search_artifact`.

- Use two ordinary public objects with a real initial index. Keep selected project config permitting both throughout; write a separate explicit `gateway.yaml` also initially permitting both. Start a real child with that exact file and prove semantic+why target success and safe vectors.
- Rewrite only gateway.yaml to add target exclusion. Make a bounded pre-restart request demonstrating the live process still has its original accepted binding (it may still return the target). This documents ADR0065, not a stale-read regression; operator revocation becomes effective through the required restart. Do not claim file editing hot-revokes an existing process.
- Explicitly `drop(server)`, spawn a new child with `spawn_with_config(root, Some(&gateway_path))`, and initialize. No graph/search/source or selected project config changes. Reissue semantic, hybrid and why. Target is absent, safe retains a vector, why has the existing not-found envelope and no target body/owner; no new drift diagnostic for hidden-only exclusion. Project config and the harness's ambient restricted variables must not override narrow startup authority.
- Assert retained Search Artifact, source and graph bytes unchanged, and safe why full payload unchanged across the two processes. Do not assert differing process IDs as the sole restart proof (PID reuse is possible); Drop's kill/wait plus a fresh spawn is the lifecycle operation.

## Existing seams and nonchanges verified

- `adoc-local/src/use_cases.rs:894` graph loader, `:940` why session loader, and search per-call loader construct fresh sessions. `resolve_graph_artifact_for_read` discovers config when retrieval policy is needed even with an explicit artifact.
- `adoc-mcp/src/main.rs:105` reads gateway policy before constructing/serving the process; `lib.rs:461` creates a fresh LocalContext and passes its stored policy override. No mutable long-lived RetrievalSession was found.
- `adoc-core/src/application/retrieval.rs:255–299` resolves each entry against the current projected graph, recomputes existing embedding composition hash, and filters before VectorIndex. Hidden IDs do not induce drift diagnostics; present permitted stale compositions do.
- `semantic_stale_vectors_cli.rs` already supplies the exact real-old-search/new-graph-only precedent, partial semantic warning and hybrid lexical-only stale candidate assertions. Leave its ordinary three-object test unchanged.
- `gateway_field_projection_cli.rs` pins omitted owner, retained public body and canonical content_hash under current projection. Verify body-empty shape from actual serializer when implementing; do not invent a new field marker.
- `stdio_gateway_binds_explicit_policy_once_independently_of_selected_project` already proves startup binding survives an invalid rewritten file. Extend lifecycle coverage alongside it, without weakening its existing sensitive audit refusal.

## Execution order, checks and repair boundary

After root accepts a consolidated T4 plan and verifies T3 completion/new base, add the three lifecycle tests in the two files and run against unchanged T3 production. Existing behavior may pass immediately: report coverage-first evidence accurately, not fabricated failing-first evidence. If a test fails, first distinguish fixture/provider/config/assertion mistakes from an actual stale admission defect. Escalate any confirmed production gap with exact before/after files and responses; root assigns only its narrow responsible shared loader/admission change. No proactive production refactor.

Proposed focused commands, to run only after explicit shared-target handoff:

```sh
CARGO_TARGET_DIR=/tmp/adoc-e6-2-t2-target cargo test -p adoc-cli --test gateway_parity --test semantic_stale_vectors_cli --locked
CARGO_TARGET_DIR=/tmp/adoc-e6-2-t2-target cargo test -p adoc-mcp --test stdio_dogfood --locked
```

Root/core final integration retains repository gates: staged prek (fmt/clippy/workspace tests), workspace build/docs and unchanged explicit e6_2_compatibility baseline gate. Existing gateway audit/spool tests remain full-suite regression coverage; T4 must not delete/rewrite history. Preserve distinct `E6.3.T4-*` command logs, exact exits/counts/ignored timing disclosure, source and runtime hashes, and fixture old/new artifact byte/hash assertions. Do not overwrite T3/T2 evidence or rerun shared Cargo while another worker owns it.

Independent reviewer should check: retained old artifact really used; permission-only case keeps source/hash unchanged; same-process field case truly reloads graph; restart case does not silently demand hot reload; safe vectors remain useful; wrong-kind/composition admission stays before scoring; ordinary full-payload compatibility remains exact. Root pairs this local proof with Cloud E2.6 invalidation/current-contributor finalization coverage; local tests alone do not claim Cloud revocation acceptance.

No material domain decision remains for this bounded plan. Rollback consists of reverting test/documentation additions if production already satisfies them; no migration or user data operation is proposed. Any need for live gateway policy refresh or persisted Cloud vector invalidation is a separate architectural expansion.

## Cloud real-runtime lifecycle

Reuse E6.3.T4-preflight.md and accepted E2.6.T4 invalidation contract. Cloud has no persistent vector producer/cache to invalidate. The concrete extension connects current ACL invalidation to actual pinned Rust index output and the complete manifest, then proves the next request rebuilds from current authority. No production migration or cache-generation mechanism is justified without a failing lifecycle.

Own the existing `tests/e2e/managed-retrieval.spec.ts` lifecycle step and only necessary existing managed-retrieval integration assertions. Its first test already compiles real nodes, overrides the native fixture candidate producer before immutable admission, creates genuine GoTrue rows and uses real HTTP. Reuse nested `insertFixture`, `promoteHidden`, `token`, `sql`, `response`, and the actual runtime adapter `invokeManagedRetrieval` with `runtimePinFromEnv` if direct buffered output is needed. Do not use the raw SQL-only candidate_node fixture with Rust: it intentionally hashes kind:body, not the real v6 governed meaning. Existing E2E compiler-node substitution is the accepted proof seam.

Add two bounded lifecycle scenarios, reusing fresh fixture coordinates so existing timing/hidden-presence proofs remain untouched:

1. Prepare actual native input under current source ACL, invoke the pinned Rust adapter and assert a nonempty valid result plus its returned contributor bindings. Record `public.record_source_acl_permission_change` for the exact snapshot workspace/connector/principal/container/kind/id. Call native `finalize_managed_retrieval` with the real complete returned bindings: false/refusal means the buffered old output is unusable. Assert the existing source-evidence/session invalidation hook marks the applicable current facts, while immutable snapshot/history bytes remain unchanged. A new actual HTTP request with the old valid login must return no revoked object, using a fresh current input/index. An independently scoped authorized fixture remains readable; retain exact allowed body/hash as its control.
2. With both genuine compiled active objects authorized, issue a limit-one search through the actual runtime and retain its complete manifest. Revoke knowledge.read on the contributor absent from the returned hit. Old complete bindings must fail finalization while the hit-only binding still has authority; a subsequent actual HTTP search returns the safe hit with a fresh manifest omitting the revoked contributor. This extends the existing real second-session non-hit race with actual Rust index material, not a mocked manifest. Select the non-hit from actual output rather than assuming lexical tie order. Native privileges, grants and source ACL remain the same existing engine.

Critical operations reuse existing signatures:

```ts
const input = JSON.parse(sql(`select public.prepare_managed_retrieval(${authority});`));
const result = await invokeManagedRetrieval(pin, Buffer.from(JSON.stringify(input)), search);
// require result.ok; keep original responseBytes and result.contributingBindings
// record scoped native ACL or object-grant revocation
// finalize(..., quote(JSON.stringify(result.contributingBindings))::jsonb) must refuse
// actual new HTTP retrieval evaluates current input independently
```

No production test hook, artificial success, modified semantic hash, external vector service, blanket cache deletion or mutable audit cleanup. If current behavior passes on the completed T3 baseline, record coverage-first acceptance honestly; do not manufacture a failing test or unnecessary production patch. If a real failure emerges, diagnose and repair the shared responsible layer within accepted scope.

Final gates reuse Cloud required logical checks and actual retained T4 runtime/HTTP proof, isolated Cloud_e6 only. Native source-ACL and managed-retrieval checks carry only where exact source is unchanged; run new lifecycle and affected concurrency tests, then full E2E and source-bound final checks. Keep original immutable history and current-clock race evidence. Hosted CI still has no unpublished Rust binary; local actual runtime checks are explicit required evidence. Root owns final paired pins, workflow/review IDs, PRs and CI; no merge/deploy. No unresolved domain question was identified.

## Coordinator final gates

Adoc staged `prek run --verbose`, `cargo build --workspace --locked`, `cargo doc --workspace --no-deps --locked`, and explicit retained pre-E6 graph-v6 producer comparison remain required. Record meaningful source equivalence if unchanged shipped code reuses a binary; never overwrite older evidence. Root uses actual pinned binaries through runtime/HTTP/native tests and retains complete old/new artifact and manifest evidence. Existing optional performance checks remain separate.

Required Cloud logical checks: `pnpm lint`; `pnpm format:check`; `pnpm typecheck`; `pnpm test`; `supabase db reset`; `pnpm test:db`; `pnpm build`; `pnpm test:e2e`; `bash scripts/domain-authority-scan.sh`; `bash scripts/contract-scan.sh`; `bash scripts/ui-roadmap-check.sh`. Use only `/tmp/agentdoc-e6-supabase` / `supabase_db_cloud_e6`, API55321/DB55322/app3116; never U1's shared database. Reset before repeated committed fixed-ID integration/native suites. Current immutable rows are preserved; newly recorded revocation history is append-only. Exact source binding permits carrying unchanged checks, but newly added lifecycle/race paths require actual execution.

Freeze paired commits before independent review of old-artifact admission/current ACL/complete non-hit manifest/safe controls/restart boundary. Preserve finding IDs and dispositions, adjudicate and scope any repair. Publish paired stacked PRs only after checks/review; verify remote exact registry and hosted CI before slice completion. Hosted CI does not build unpublished Rust; local real runtime checks are mandatory and separately retained. No merge/deploy. On completion continue the next dependency-ready E6 slice using fresh live state, not assumed roadmap completion.
