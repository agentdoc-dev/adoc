# E6.3.T3 — Exclude protected material before embedding generation

Accepted scope under the continuing E6 instruction; T2 hosted CI34129583862 passed before this execution. Root owns the durable run, findings, checkpoints, commits and PRs. Independent Codex specialist review required; Claude waived. Limits remain two repair rounds/four review attempts, with diagnosis on exhaustion.

Verified fresh paired worktrees: Adoc `.worktrees/e6.3-t3` from `21a4ebf07742ae937e160b1fa19878bbaff8ec21` (open PR226), Cloud `.worktrees/e6.3-t3` from `61521aba3621239bcd3fdf66e99a9f7df5ed695d` (open PR96). Branch `feat/e6.3-t3-embedding-exclusion`, planned PR base `feat/e6.3-t2-audit-spool`. Live remote heads and absence of a prior T3 branch were verified before worktree creation. Reference worktrees were not development instructions.

Authority: ROADMAP-V10 current hierarchy → v10 README → EXECUTION-MAP E6.3 → MILESTONES T3, with accepted PRD1.1§7/A4/A8/K7 and existing ADR0039/0040/0056–0058. No discovery/domain restart. Aw-domain inspection found no terminology or invariant gap: absent policy is public, authored field floors and current permission ceilings already apply. Aw-context records the existing projection and composition relationship. This plan consolidates actual-code preflights E6.3.T3-adoc-preflight.md, cloud-preflight.md and surface-plan.md.

## Objective and authority

Apply the existing permission/field projection **before** passage composition, embedding cache matching and provider inference, so the new Search Artifact contains no policy-excluded object/carrier ID or vector. Preserve readable public siblings when only selected fields are protected. Preserve exact KO/prose composition formulas, `adoc.search.v2`, canonical graph bytes and ordinary unclassified artifact/output bytes. T3 follows accepted MILESTONES line741 / E6.3 execution map (depends E6.1); spool is not an embedding dependency. T4 separately owns long-lived derived-material invalidation/rekey.

No new domain decision is needed. `RetrievalPolicy::permits` in `domain/retrieval/mod.rs:57–80` explicitly returns public-only for None; absence of object visibility is canonical public. `application/field_projection.rs:104–109` independently supplies the same public policy for absent policy while applying authored floors. Do not reinterpret None as unrestricted or manufacture a new policy type.

## Minimal production changes and exact call sites

Core worker owns:

- `crates/adoc-core/src/application/search_artifact.rs`: add the optional policy argument, project one cloned graph before any cache/composition/provider operation, adapt its one private test helper call at389, and add direct producer tests only if compile's provider spy cannot cover the same behavior.
- `crates/adoc-core/src/application/compile.rs`: forward policy at the **two** calls at382 and424 (Enabled test provider and production EnabledFactory), revise the policy documentation at BuildInput line36, narrow the earlier rendering-only equality test at932, and add failing-first recording-provider/cache tests using its existing helpers.
- `crates/adoc-core/src/application/retrieval.rs`: tests only, for stale wide vectors against the projected graph if the existing admission tests do not already express the new artifact scenario. Production read/rank logic remains unchanged absent a reproduced gap.

Illustrative implementation with verified current names:

```rust
use crate::domain::retrieval::{RetrievalPolicy, metadata};
use super::retrieval::filter_retrieval_document;

pub(crate) fn build_search_artifact(
    graph_document: &GraphArtifactDocument,
    graph_json: &str,
    provider: &dyn EmbeddingProvider,
    prior_search_artifact_path: Option<&PathBuf>,
    policy: Option<&RetrievalPolicy>,
) -> Result<SearchArtifactBuild, Vec<Diagnostic>> {
    let mut searchable = graph_document.clone();
    filter_retrieval_document(&mut searchable, policy)
        .map_err(|diagnostic| vec![*diagnostic])?;
    let model = search_model_header(provider);
    let cache_load = load_matching_search_cache(prior_search_artifact_path, &model);
    let graph_artifact_hash = sha256_prefixed(graph_json.as_bytes());
    // Existing KO and prose loops consume &searchable instead of graph_document.
    // Existing composition/hash/cache/output code stays unchanged.
}
```

In `compile.rs`, capture the small owned policy before the existing mutable embeddings match, avoiding a larger dispatch refactor:

```rust
let search_policy = build_options.as_ref().and_then(|options| options.policy.clone());
// ... existing build_options.as_mut().map(|options| &mut options.embeddings) ...
build_search_artifact(
    &graph_document, &graph_json, provider, prior_search_artifact_path.as_ref(),
    search_policy.as_ref(),
)
```

The test branch passes `*provider`, production passes `provider.as_ref()` as today. Model construction itself receives no source passages; no new provider factory is necessary. Existing `build_invalid_policy_refuses_before_source_or_embedding_provider_calls` remains intact.

The clone uses `filter_retrieval_document` (`retrieval.rs:407`), not a new visibility filter: it invokes authored scalar projection, object allow/exclusion checks, copied reference/prose closure and lifecycle refresh. Original graph/hash identity never changes. Projection errors propagate through the existing SearchArtifactBuild diagnostic path and prevent any passage call. Source/compiler errors already stop artifact production.

## Composition, cache and rank invariants

Do not edit `domain/retrieval/metadata.rs:61–98` formulas or normalization. KO composition remains kind + normalized body + ID/status-or-severity-or-trust/owner. Prose remains normalized text + page marker; code/minimum-five-token controls stay. A public partial KO can retain its own ID and a vector composed from empty protected body / unknown owner plus safe fields. An excluded object or a carrier removed by existing closure has neither output entry nor vector.

The prior Search Artifact is input, potentially produced under a wider policy. Only the projected current corpus enters cache matching/miss collection; no excluded cached entry is copied to new output, and no old protected composition is reused for a newly redacted entry. Preserve matching model/dimension/hash rules and prose content-hash reuse across block renumbering. Safe unchanged inputs still reuse cached vectors. Keep `graph_artifact_hash` bound to the **actual full graph_json**; do not hash a secret alternate graph or add policy fields to the search schema.

Current `retrieval.rs:255–309` rejects absent IDs and mismatching kind/composition before `VectorIndex::new`; keep it. Current HybridRanker is RRF over filtered candidate IDs, not a learned/external reranker. No separate rerank provider exists in the inspected crates; do not add one. Queries embedded through the existing query provider are user input, not corpus passages. T3 proof covers corpus passage and vector admission boundaries; T4 retains long-lived cache invalidation responsibility.

## Failing-first core proof

Use existing `InMemorySourceProvider`, `source_file`, `fixed_today`, `build_with_provider_for_date` and `RecordingEmbeddingProvider` at `compile.rs:1696`. No new model/dependency/fixture framework. Use one compact fixture shared by these assertions: internal KO; explicit-excluded public KO; public partial KO with `owner: HIDDEN_OWNER_CANARY` and `field_visibility: owner=internal, body=restricted`; safe public sibling; safe eligible prose and prose referring to the hidden KO.

1. **Provider boundary red first:** build public/default using RecordingEmbeddingProvider. Assert no hidden KO ID/body/owner or excluded-reference prose occurs in `recorded_inputs()`; assert the exact public partial input with empty body/unknown owner and the exact safe sibling input. Inspect output IDs: partial and safe survive, hidden/excluded carriers do not. Current producer fails because it embeds the full graph. Also assert provider inputs/output for explicit internal/restricted authority retain only what that policy permits. This establishes None=public in the actual producer, not just predicate tests.
2. **Wide cache proof:** build the fixture under explicit broad authorized policy with provider model `recording/test`; save the real Search Artifact. Optionally change hidden body/owner to make graph/header drift concrete, then narrow to public with the excluded ID and rebuild against the saved artifact using a fresh RecordingEmbeddingProvider of the same model/dimension. Assert excluded IDs absent, safe entry hash/vector unchanged and reused, partial entry hash/vector equals a clean public build and differs from broad cache. Assert misses contain only safe projected compositions. Do not infer absence of secret text merely from searching final vector JSON.
3. **Exact graph identity:** for same source/date, public and broad graph JSON stay byte-equal; each search header hashes its actual original graph bytes. Rename/narrow `build_rendering_policy_does_not_mutate_graph_or_search_artifacts` to retain graph equality but require policy-appropriate search IDs. Healthy unclassified search bytes remain exact.
4. **Drift/ranking:** reuse existing `vector_admission_checks_current_composition_and_kind_on_drift` at1937 and prose counterpart at2000. Align a poisoned old wide vector to the test query; load current public graph with that old artifact. The hidden ID and old partial hidden-field composition acquire no semantic/vector rank. Hybrid may keep a safe lexical partial result, without its stale vector. Preserve the existing rule that hidden-only drift cannot create observable diagnostics, while stale permitted composition may warn.

Existing search-artifact prose tests (entry kinds394, renumbered cache463, subthreshold501, old schema cache523) remain regression controls. Explicitly include a safe cache-hit count so “never reuse any cache” cannot pass the exclusion test.

## Independent surface ownership (two existing files)

Surface worker owns **only** `crates/adoc-cli/tests/gateway_parity.rs` and the necessary fixture hunk in `crates/adoc-cli/tests/permission_parity_cli.rs`; root accepted this additional hunk after inspection. No production CLI/MCP/local plumbing change is needed. Root owns accepted plan/ADR/context and Cloud pins/checks. Workers preserve each other's edits and coordinate the shared Cargo target; no nested delegation/state/commits.

Use gateway_parity's existing `TestWorkspace`, deterministic config, `write_policy`, `cli`, `mcp_result` and exact-envelope `assert_bytes`. The surface proposal specifies two real build/artifact cases: public trusted MCP build overriding broad project config, and a broad-cache→narrow rebuild through actual CLI output paths. Exercise both configured and explicit output paths; include bounded CLI --audience control retaining configured exclusions, and skip-mode no-new-search control in a fresh output directory. The partial KO remains vector-eligible from projected content. Assert exact permitted KO/prose IDs and graph/header binding; use core spy for actual input confidentiality. Keep authorized-sensitive MCP refusal without audit setup; CLI provides the positive sensitive-authorized read control.

Two existing **classified setup fixtures** intentionally need explicit authorized build:

- `gateway_parity.rs:85–97` builds with policy absent then tests internal semantic/hybrid ranks. Write a temporary broad build policy before that single build and restore its original config afterward. Retain all later read-policy and sensitive-refusal assertions.
- `permission_parity_cli.rs:699–740` `matching_manifest_and_stale_hidden_vector_have_complete_index_parity` builds under public CONFIG but requires a real internal embedding to poison at733. Temporarily choose an internal-authorized build policy (existing allowed set already includes internal), produce that wide test artifact, then restore public CONFIG before all parity queries. Keep the test's same wide index in present/absent graph worlds and all no-warning/semantic-rank assertions. Do not remove the hidden vector from this deliberately adversarial fixture or weaken its unwrap to conceal the changed setup.

Other inspected authored classified fixtures are HTML-only `--no-embeddings` controls (`build_audience_cli`), typed raw-graph retrieval fixtures, or MCP audit/reference tests building with no embeddings; they do not require broader vector generation. Adjust another fixture only if an actual failure demonstrates the same necessary setup assumption.

## Historical ordinary compatibility is unchanged

`e6_2_compatibility.rs:96` copies `tests/fixtures/v0_6/project`; source inspection found no authored visibility/field_visibility in that corpus. Its recursive guard at39–57 rejects all classification/projection/declassification/audit opt-ins. This fixture stays default/None, with unchanged raw HTML/graph/search goldens and unchanged independent producer manifest. Do not authorize it broadly, regenerate goldens, strip fields or equate the unpublished pre-E6 graph-v6 commit with a release. Existing baseline remains e1b74a3f579933e3f8194c6c998f91a24578e9db / retained binary SHA9b4ad1331514d79f5c772ad3ba2ad73cbcd2c25bf828afe8fa2f6b71b6476d6f. Ordinary stdio exact-byte regression also remains untouched.

## Checks, evidence and review

First preserve the failing provider-spy and real artifact exclusion tests. Then run scoped core producer/retrieval tests plus both surface files; semantic_search_cli, semantic_stale_vectors_cli and stdio_dogfood verify the unchanged rank/protocol paths. Reuse one shared target with explicit worker handoffs. Once source is frozen, required staged `prek run --verbose` (fmt/clippy/workspace tests), `cargo build --workspace --locked`, `cargo doc --workspace --no-deps --locked`, and explicit historical gate:

```sh
ADOC_RETRIEVAL_BASELINE_BIN=<evidence>/adoc-pre-e6-graph-v6-runtime \
CARGO_TARGET_DIR=/tmp/adoc-e6-2-t2-target \
cargo test -p adoc-cli --test e6_2_compatibility --locked -- --include-ignored
```

Record actual counts/skips, command exits, source hashes and distinct T3 binaries without overwriting T2 evidence. No new performance/model-download gate; retained optional timing benchmarks remain separately disclosed. Root refreshes the exact Adoc contract pin and compatibility assertion together after commit and runs Cloud existing checks/runtime parity. Current managed Cloud semantic mode has no vector producer/index and hybrid degrades to lexical; no native migration/API expansion is justified by this slice.

Independent specialist review should focus on before-inference filtering, wide-cache resurrection, safe partial composition, hash/ordinary byte compatibility and preserving authored policy authority. Claude remains waived. Root owns review limits, stable finding IDs, adjudication and PR/CI; do not merge or deploy. No checks or implementation were performed while preparing this proposal.

## Cloud integration and final acceptance

Actual Cloud `src/lib/validation-runtime/managed-retrieval.ts` invokes the pinned managed retrieval command with no provider environment, embedding producer or learned reranker. No production Cloud/native change is justified. Root updates exact registry pin and assertion after the Adoc commit and binds carried native authority checks to unchanged sources. Root runs actual runtime integration and full HTTP/MCP E2E with distinct retained T3 binaries; isolated `supabase_db_cloud_e6` only, API55321/DB55322/app3116. Never shared U1 database. Reset before repeated fixed-ID native/integration suites; no cleanup mutation of immutable history.

Required Cloud logical gates remain `pnpm lint`, `pnpm format:check`, `pnpm typecheck`, `pnpm test`, `supabase db reset`, `pnpm test:db`, `pnpm build`, `pnpm test:e2e`, `bash scripts/domain-authority-scan.sh`, `bash scripts/contract-scan.sh`, `bash scripts/ui-roadmap-check.sh`. Preserve exact command exits, source equivalence for any carried unchanged checks, real runtime hashes and explicit CI skips. No Rust build in Cloud CI. Verify exact remote registry after authorized push and join hosted CI before slice completion.

Acceptance requires: protected body/owner/object/carrier absent from actual provider passages under public policy; no excluded object IDs/vectors in newly generated Search Artifact; broad cached vectors cannot resurrect hidden entries or old hidden-field composition; safe cache hits remain; explicit authorized build preserves permitted material; adversarial graph/search drift cannot produce unauthorized vector rank; formulas/searchv2/canonical full graph bytes and ordinary historical artifacts remain unchanged. Source ACL and authored floors remain ceilings, with no new lowering path. T4 owns subsequent revocation/rekey work. Publish paired stacked PRs after independent review and final checks; no merge/deploy. Then select next dependency-ready E6 slice.
