# E6.3.T1 — Authenticated gateway sensitive-access emission

Accepted for execution under the continuing E6 instruction. Root alone owns workflow state, findings, commits/PRs and checkpoints. Independent Codex specialist review is required; Claude waived. Two repair rounds/four review attempts; exhaustion requires diagnosis, never completion.

## Authority, state and scope

Reuse ROADMAP-V10 → current README authority → EXECUTION-MAP E6.3 → MILESTONES T1, accepted PRD/A3/A4/A8, ADR0065 and new ADR0066. E6.3 depends on completed E6.1. T5 reviewed stack bases preserve later field behavior: Adoc816279e937d5e34bf1800f324667c39085218a10 / Cloud7afe4ab2dbba2589950c03734c153d82d9f4b9b7, PR224/91; hosted CI34117328298 succeeded. Live PR/branch inspection found no T1 implementation. Verify remote heads before the chosen fresh .worktrees/e6.3-t1, branch feat/e6.3-t1-gateway-audit; reference worktrees are not development instructions.

T6 already implements managed v0 sensitive event, real human/native sink and classification. Reuse them unchanged. Remaining T1 is actual MCP emission, honest local subject/repository/session binding, and CLI exemption. Cloud is the final sink under nonconflicting original V10 167/3412/3444. T1 is a connected authenticated-human synchronous cut; T2 supplies disconnected/default pending/spool/recovery. No terminal-local success, fake workspace/version identity, workload credential shortcut, new permission, discovery restart or product question.

Aw-domain resolves the relevant distinction: gateway-reported local subjects are authenticated reports, not native managed authorization proof. Complete local-policy digest identifies reported policy and grants nothing. Aw-context links T1 to T6 and the gateway-reported/session-binding terms in Cloud RETRIEVAL.md. ADR0066 records the new representation and projection reuse without replacing accepted artifacts.

## Shared local field projection and typed access

Preserve **E63T1-01**, reproduced with retained T5 binary: a public claim with owner INTERNAL_OWNER_E63 and field_visibility owner=internal returns that owner to default-public why, without classification. Root retained exact source, command, bytes and binary hash in workflow evidence. This is a necessary correctness/security prerequisite, not a new product feature.

Extract reusable authored scalar projection/carrier refresh from managed_retrieval/field_projection.rs project_object/project_receipts into a shared application helper. Invoke before local indexes at retrieval.rs filter_retrieval_document_with_sources; graph/signal loaders already use that shared filter. Validate original GraphIndex/source first, preserve original hashes and source files, retain authorized siblings, empty hidden required body and omit hidden fields. Reuse dedicated-member/collision rules and fail closed for unsupported protected carriers. Keep native declassification overrides private to managed projection. Missing/invalid floors never become public.

Raw non-null dedicated-member presence must come from the same original decode (private serde-skipped sidecar if needed), not a second read or serialization of defaulted fields. Preserve removed body/resolved_by reference edges, exclusion fixed point, expiry/contradiction/evidence-quality refresh, and original malformed-source refusal. Preserve filtered embedding-composition mismatch checks so hidden data cannot affect query ranking; stored embedding generation exclusion remains T3.

Fixed private API shapes (core worker may choose module placement without changing semantics):

```rust
pub struct AccessedKnowledgeObject {
    pub object_id: String,
    pub content_hash: String,
    pub classification: Option<SensitiveClassification>,
}
pub struct ReadAccess { pub objects: Vec<AccessedKnowledgeObject> }
```

Objects are sorted/unique. Add serde-skipped read_access to local Why/Search/Graph/Stale/Contradictions/Impacted outcomes, empty on early errors. Reuse the same filtered immutable session and existing material-source attribution; no second artifact read, JSON scanner or invented prose hash. Preserve surviving typed Reference witnesses privately before GraphIndex discards them.

Typed collectors include returned KO roots and actually exposed resolved_questions, effective_reason contradiction and evidence_quality sources; exposed relations/body references; selected prose/heading references to real KOs; graph nodes/endpoints; emitted stale IDs; emitted contradictions/claims/contradiction IDs; impacted roots and actual reasons.via_source_object. Scanned sensitive nonhits do not become subjects. Removed carriers lose witnesses. Missing/inconsistent expected identity/hash/class refuses. Keep direct subject class distinct from the maximum displayed on a root. Managed native contributor reauthorization and exact coordinates remain unchanged.

## Gateway-only exact successor

Register adoc.sensitive_access.v1 without changing v0 bytes/schema or managed consumers. Move the registry's unknown-successor fixture to v2 and test cross-version refusal. Publish schema/resource/validator parity.

```json
{"schema_version":"adoc.sensitive_access.v1","event_id":"<lowercase UUID>","context":{"kind":"gateway_repository","workspace_id":"<UUID>","repository_id":"<UUID>"},"caller":{"principal_id":"<UUID>","auth_session_id":"<UUID>","gateway_session_id":"<UUID>"},"reporting_basis":"gateway_reported","command":"search","local_policy_digest":"sha256:<64 lowercase hex>","sequence":1,"objects":[{"object_id":"guide.field","content_hash":"sha256:<64 lowercase hex>","classification":"internal"}]}
```

Closed root/nested object shapes, not positional arrays; canonical lowercase UUID/digests and existing Object ID grammar. Commands search/why/graph/stale/contradictions/impacted_by. Subjects 1..1000, ordered distinct, internal/restricted only; sequence1..9007199254740991; exact UTF-8 event ≤1MiB. Overflow refuses, never truncates. No bodies/query/path/credential/timestamp or fake canonical/version IDs. Validate immutable event builder and complete canonical effective-policy digest in core. Sorting may canonicalize caller input but never silently deduplicate conflicting subjects.

## Trusted MCP startup and transport

Preserve --config PATH retrieval semantics; add explicit operator-only --audit-config PATH closed JSON:

```json
{"cloud_url":"https://cloud.example","workspace_id":"<UUID>","repository_id":"<UUID>","bearer_token_file":"<operator credential path>"}
```

Bind one canonical served root; an override must canonicalize identically. Native response supplies actual human/auth-session/gateway-session IDs. Tool arguments, ambient project config and client name cannot replace binding/policy/sink. Policy mutation clears or invalidates a bound auditor. Credentials have no revealing Debug/log/error representation. Public default/no-sensitive behavior remains unchanged; sensitive output without trusted recording context refuses.

Use installed ureq3.3.0 via a direct MCP dependency and standard UUIDv4 support; no custom HTTP/entropy framework. Disable redirects, set10s global timeout and64KiB response cap including chunked bodies. Fixed HTTPS origin, no URL userinfo/query/fragment; literal loopback HTTP is allowed for local integration. Bearer goes only in Authorization. Fetch current Context7 docs before library integration. Keep sync public run_* APIs and actual async handlers sharing the same audit-before-release function; spawn_blocking contains sync query/network work rather than blocking Tokio workers. Preserve search f32 serialization.

Every audited result appends `Sensitive (internal).` or `Sensitive (restricted).` using its highest actual subject class. Existing KO labels remain; closed graph/signal/prose envelopes stay unchanged, including prose with sensitive references. Ordinary CallToolResult has no extra block and keeps its bytes.

Use the registered refusal **retrieval.audit_sink_unavailable** in a typed MCP error with machine-readable code and fixed message; do not invent mcp.audit_unavailable or echo upstream errors. Core unresolved attribution uses existing visibility refusal. T2 pending warning is deferred.

## Native/API admission and fresh transmission

Two existing-style authenticated-human routes:

```text
POST /api/v1/workspaces/:workspaceId/gateway-audit-sessions
 body {repository_id,setup_request_id,local_policy_digest}
POST /api/v1/workspaces/:workspaceId/gateway-audit-sessions/:sessionId/events
 exact bounded event bytes; x-agentdoc-egress-policy-digest header
```

Reuse authenticateApiV1Human, boundedJson (retains exact bytes), correlation/no-store/error helpers. Identity is derived before admission, never body supplied. Validate native success shapes and binding/receipt correspondence. Uniform 42501 denial, 22023 invalid request, 23505 conflict, fixed503 unavailable; no raw SQL/content. No UI routes/components change.

Service-only RPCs:

```sql
open_gateway_sensitive_access_session(p_workspace_id uuid,p_repository_id uuid,
 p_setup_request_id uuid,p_local_policy_digest text,p_principal_id uuid,p_auth_session_id uuid)
record_gateway_sensitive_access(p_workspace_id uuid,p_gateway_session_id uuid,
 p_event_bytes text,p_egress_policy_digest text,p_principal_id uuid,p_auth_session_id uuid)
```

Immutable forced-RLS sessions bind real registered repository, human/auth session, setup retry ID and local-policy digest. Immutable events retain exact bytes/digest, safe session sequence and sink received_at. Revoke direct table writes/reads and guard update/delete/truncate. Existing managed sink remains separate. Setup retry returns same immutable binding only after current authority; different repository/policy under same retry key conflicts. Event IDs and session sequences serialize; exact bytes replay only after current checks, different bytes/sequence conflict. Native latest+1 enforces ordering. No FK may destroy audit history with auth-session retention.

Return setup's flat immutable gateway_session_id/workspace_id/repository_id/principal_id/auth_session_id/local_policy_digest plus separate fresh transmission {audit_metadata:true,egress_policy_digest}. Replay setup immediately before EVERY event attempt/retry; no subject body on failed preflight. Pass fresh digest outside immutable event bytes. Event receipt is closed {event_id,event_digest,gateway_session_id,sequence,recorded:true,replayed:boolean}; verify exact correspondence before release.

Reuse current human-session check, repository-scoped knowledge.read, lock_canonical_authority_inputs and latest_egress_policy with existing egress scope lock namespace. READ COMMITTED; new table/advisory/repository/authority/event locks in consistent order before final clock checks. Current identity/grants and latest exact audit_metadata digest rechecked after waits and insertion; expiry/refusal rolls back. No cached wider policy or durable permit. A preflight is not a lease across the network gap: subsequent drift refuses admission/output, not a false claim that metadata was unsent.

One shared mutex protects sequence and at most one in-flight exact event. Ambiguous acknowledgement retains original event ID/bytes/sequence; resolve it with fresh preflight before a new event. Advance only on matching receipt. Never release stale withheld content after recovering its event; a newly prepared response requires its own event. No durable spool, unbounded retry or silent skipped sequence.

## Ownership, checks and completion

Core worker owns shared projection/typed access, v1 domain/schema, local outcome plumbing and core/local/CLI tests/registry/resources/docs. MCP worker owns gateway_audit.rs, MCP lib/main, MCP Cargo.toml/lock, adapter/stdio tests and gateway guide; coordinate fixed ReadAccess/event-builder exports, do not edit each other's files. Native worker owns new migration + pgTAP fixtures only. Root owns Cloud API/route tests/real stdio-HTTP-native E2E/concurrency integration, pin/assertion, plan/ADR/domain/context, all DB execution, state/commits/PRs. Workers are not alone, preserve edits and do not delegate/commit/push. Shared Cargo target access is coordinated; final full gates run on frozen combined source.

Acceptance: E63T1-01 real red→green with public siblings/hash preservation; all six exact typed subject sets and one graph read, both sensitive classes, copied/reference/prose cases, no-hit/public/denied no event; strict v1/v0 schema parity/bounds; real stdio→authenticated HTTP→native immutable recording before output; no CLI audit; caller/root/policy spoof refusal; shared sequence/concurrency/replay/ack-loss; fresh egress deny prevents body send, policy race rejects admission; native expiry/revoke after waits/insertion rolls back; sink failure yields typed refusal; original no-visibility artifacts/MCP bytes remain stable. Mock success alone is insufficient.

Adoc required: focused core/local/CLI/MCP/schema/registry tests, explicitly invoked T5 historical producer gate; staged prek (fmt/clippy/workspace/hygiene), cargo build --workspace --locked, cargo doc --workspace --no-deps --locked, git diff checks. Retain adoc AND adoc-mcp binaries with source/hash evidence. Cloud exact required commands: pnpm lint; pnpm format:check; pnpm typecheck; pnpm test; supabase db reset; pnpm test:db; pnpm build; pnpm test:e2e; bash scripts/domain-authority-scan.sh; bash scripts/contract-scan.sh; bash scripts/ui-roadmap-check.sh. Use isolated cloud_e6 exact-source DB, never shared U1 DB; native changes require fresh full native checks. Record actual skips, never count them as passes.

Freeze commits before independent specialist review of projection/typed attribution, native auth/concurrency and transport secrecy/freshness. Preserve E63T1 finding IDs, adjudicate fixes and scoped rereviews within limits. Complete required checks, checkpoint PR/CI evidence and next action; create paired stacked PRs, cancel waived Claude, join CI, then T2. No merge/deploy. Rollback refuses new gateway recording; immutable history is never deleted or relabeled.
