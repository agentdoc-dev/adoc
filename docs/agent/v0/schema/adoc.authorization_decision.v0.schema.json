{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "adoc://agent/v0/schema/adoc.authorization_decision.v0.schema.json",
  "title": "AgentDoc Authorization Decision",
  "description": "Replayable, source-neutral authorization decision for E2.2 and E2.4. The record carries the explicit evaluation time, policy version, complete scoped grant input, immutable group-membership evidence, source ACL ceiling, visibility and action-policy inputs, winning basis, and allow/deny/insufficient_context result. Schema validation pins fail-closed structural invariants and the top-level freshness precedence; the Cloud evaluator owns lower-gate precedence, grant applicability, scope matching, membership effectivity comparisons, expiry evaluation, replay semantics, and exact copying of winning-grant fields, including group provenance, into the basis. An absent group on a grant means that grant is principal-attributed.",
  "type": "object",
  "required": [
    "schema_version",
    "principal",
    "permission",
    "resource",
    "evaluation_time",
    "consequential",
    "hard_deny",
    "grants",
    "membership_evidence",
    "source_acl_ceiling",
    "visibility",
    "action_policy",
    "policy_version",
    "result",
    "reason",
    "basis"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "const": "adoc.authorization_decision.v0" },
    "principal": { "$ref": "#/$defs/principal" },
    "permission": { "$ref": "#/$defs/permission" },
    "resource": {
      "description": "Complete scope of the resource being authorized.",
      "$ref": "#/$defs/scope"
    },
    "evaluation_time": {
      "description": "Evaluator timestamp input. Schema validation pins the lexical RFC 3339 shape after identity freshness passes; identity_expired, identity_context_missing, and evaluation_time_invalid preserve unconstrained raw input for replay. Calendar validity and instant comparison are evaluator-owned and fail closed.",
      "type": "string"
    },
    "consequential": { "type": "boolean" },
    "hard_deny": {
      "description": "System-level deny observed ahead of the source ACL ceiling. True always forbids a top-level allow; unless identity_context_missing, identity_expired, or evaluation_time_invalid won earlier, it also forces reason hard_deny and a deny result. Conversely, reason hard_deny requires hard_deny true and a null basis. Because the hard-deny gate precedes the source ACL ceiling, reason hard_deny may win before an attempted required check and omit source_acl_ceiling.check_context.",
      "type": "boolean"
    },
    "grants": {
      "type": "array",
      "items": { "$ref": "#/$defs/grant" }
    },
    "membership_evidence": {
      "description": "Required scoped-grants-stage input status. current means every membership input needed for this decision was established from retained current state, whether present or absent; insufficient_context means at least one potentially grant-conferring membership input was unavailable or not yet established; not_applicable means no membership input could affect the scoped grants. A group-bearing grant requires current. no_grant with current requires immutable membership_absence_evidence, while no_grant with not_applicable forbids it. membership_evidence_unavailable requires insufficient_context, so replay can distinguish uncertainty from confirmed absence.",
      "enum": ["current", "insufficient_context", "not_applicable"]
    },
    "membership_absence_evidence": {
      "description": "Exact retained negative membership facts used by a current no_grant decision. The array contains every relevant manual group or grant-conferring external binding whose confirmed absence affected scoped-grant evaluation; evaluator-owned completeness checks fail closed. Manual entries replay against retained membership lifecycle history for the envelope principal and group at evaluation_time. External entries retain the exact negative observation, binding epoch, identity link, and source event or run.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "$ref": "#/$defs/membershipAbsenceEvidence" }
    },
    "source_acl_ceiling": { "$ref": "#/$defs/sourceAclCeiling" },
    "visibility": { "$ref": "#/$defs/policyInput" },
    "action_policy": { "$ref": "#/$defs/policyInput" },
    "policy_version": { "$ref": "#/$defs/nonEmpty" },
    "result": { "$ref": "#/$defs/decision" },
    "reason": {
      "description": "Evaluator outcome reason. evaluation_time_invalid is raised only after identity freshness passes; timestamp parsing itself is evaluator-owned so an invalid raw input remains recordable for replay. membership_evidence_unavailable distinguishes an unavailable or not-yet-established membership input from a confirmed no_grant result at the scoped-grants stage.",
      "enum": [
        "identity_context_missing",
        "identity_expired",
        "evaluation_time_invalid",
        "hard_deny",
        "source_acl_denied",
        "source_acl_stale",
        "source_acl_invalidated",
        "source_acl_unavailable",
        "membership_evidence_unavailable",
        "no_grant",
        "explicit_deny",
        "visibility_denied",
        "visibility_unavailable",
        "action_policy_denied",
        "action_policy_unavailable",
        "allowed"
      ]
    },
    "basis": {
      "oneOf": [
        { "type": "null" },
        { "$ref": "#/$defs/basis" }
      ]
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "grants": {
            "contains": { "required": ["group"] }
          }
        },
        "required": ["grants"]
      },
      "then": {
        "properties": {
          "membership_evidence": { "const": "current" }
        }
      }
    },
    {
      "if": {
        "properties": {
          "principal": {
            "properties": { "type": { "const": "human" } },
            "required": ["type"]
          }
        },
        "required": ["principal"]
      },
      "then": {
        "properties": {
          "grants": {
            "items": {
              "if": {
                "properties": { "source": { "const": "direct_grant" } },
                "required": ["source"]
              },
              "then": { "required": ["exceptional_reason"] }
            }
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "reason": {
            "not": {
              "enum": [
                "identity_context_missing",
                "identity_expired",
                "evaluation_time_invalid",
                "hard_deny"
              ]
            }
          },
          "source_acl_ceiling": {
            "properties": { "required": { "const": true } },
            "required": ["required"],
            "not": { "required": ["current_authorization"] }
          }
        },
        "required": ["reason", "source_acl_ceiling"]
      },
      "then": {
        "properties": {
          "source_acl_ceiling": { "required": ["check_context"] }
        }
      }
    },
    {
      "if": {
        "properties": {
          "source_acl_ceiling": {
            "anyOf": [
              {
                "properties": { "required": { "const": true } },
                "required": ["required"]
              },
              { "required": ["snapshot_id"] }
            ]
          }
        },
        "required": ["source_acl_ceiling"]
      },
      "then": {
        "properties": {
          "resource": {
            "required": ["connector_id", "source_container_id", "resource"]
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "principal": {
            "properties": { "freshness": { "const": "expired" } },
            "required": ["freshness"]
          }
        },
        "required": ["principal"]
      },
      "then": {
        "properties": { "reason": { "const": "identity_expired" } }
      }
    },
    {
      "if": {
        "properties": {
          "principal": {
            "properties": {
              "freshness": { "const": "insufficient_context" }
            },
            "required": ["freshness"]
          }
        },
        "required": ["principal"]
      },
      "then": {
        "properties": {
          "reason": { "const": "identity_context_missing" }
        }
      }
    },
    {
      "if": {
        "properties": { "result": { "const": "allow" } },
        "required": ["result"]
      },
      "then": {
        "properties": {
          "principal": {
            "properties": { "freshness": { "const": "current" } },
            "required": ["freshness"]
          },
          "hard_deny": { "const": false },
          "grants": {
            "contains": {
              "properties": { "effect": { "const": "allow" } },
              "required": ["effect"]
            }
          },
          "source_acl_ceiling": {
            "properties": {
              "result": { "enum": ["allow", "not_applicable"] }
            },
            "required": ["result"],
            "allOf": [
              {
                "if": { "required": ["current_authorization"] },
                "then": {
                  "properties": {
                    "current_authorization": {
                      "properties": {
                        "connector_available": { "const": true }
                      },
                      "required": ["connector_available"],
                      "not": { "required": ["invalidated_at"] }
                    }
                  }
                }
              }
            ]
          },
          "membership_evidence": { "enum": ["current", "not_applicable"] },
          "visibility": { "enum": ["allow", "not_applicable"] },
          "action_policy": { "enum": ["allow", "not_applicable"] },
          "reason": { "const": "allowed" },
          "basis": { "$ref": "#/$defs/allowBasis" }
        }
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "allowed" } },
        "required": ["reason"]
      },
      "then": {
        "properties": { "result": { "const": "allow" } }
      }
    },
    {
      "if": {
        "properties": {
          "reason": {
            "not": {
              "enum": [
                "identity_context_missing",
                "identity_expired",
                "evaluation_time_invalid"
              ]
            }
          }
        },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "evaluation_time": { "$ref": "#/$defs/rfc3339Timestamp" }
        }
      }
    },
    {
      "if": {
        "properties": {
          "hard_deny": { "const": true },
          "reason": {
            "not": {
              "enum": [
                "identity_context_missing",
                "identity_expired",
                "evaluation_time_invalid"
              ]
            }
          }
        },
        "required": ["hard_deny", "reason"]
      },
      "then": {
        "properties": { "reason": { "const": "hard_deny" } }
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "identity_expired" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "principal": {
            "properties": { "freshness": { "const": "expired" } },
            "required": ["freshness"]
          },
          "basis": { "type": "null" },
          "result": { "const": "deny" }
        }
      }
    },
    {
      "if": {
        "properties": {
          "reason": { "const": "identity_context_missing" }
        },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "principal": {
            "properties": {
              "freshness": { "const": "insufficient_context" }
            },
            "required": ["freshness"]
          },
          "basis": { "type": "null" }
        },
        "allOf": [
          {
            "if": {
              "properties": { "consequential": { "const": true } },
              "required": ["consequential"]
            },
            "then": {
              "properties": { "result": { "const": "insufficient_context" } }
            },
            "else": {
              "properties": { "result": { "const": "deny" } }
            }
          }
        ]
      }
    },
    {
      "if": {
        "properties": {
          "reason": { "const": "membership_evidence_unavailable" }
        },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "membership_evidence": { "const": "insufficient_context" },
          "basis": { "type": "null" }
        },
        "required": ["membership_evidence"],
        "allOf": [
          {
            "if": {
              "properties": { "consequential": { "const": true } },
              "required": ["consequential"]
            },
            "then": {
              "properties": { "result": { "const": "insufficient_context" } }
            },
            "else": {
              "properties": { "result": { "const": "deny" } }
            }
          }
        ]
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "no_grant" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "basis": { "type": "null" },
          "membership_evidence": { "enum": ["current", "not_applicable"] },
          "result": { "const": "deny" }
        },
        "required": ["membership_evidence"],
        "allOf": [
          {
            "if": {
              "properties": { "membership_evidence": { "const": "current" } },
              "required": ["membership_evidence"]
            },
            "then": { "required": ["membership_absence_evidence"] },
            "else": {
              "not": { "required": ["membership_absence_evidence"] }
            }
          }
        ]
      },
      "else": {
        "not": { "required": ["membership_absence_evidence"] }
      }
    },
    {
      "if": {
        "properties": {
          "reason": { "const": "evaluation_time_invalid" }
        },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "principal": {
            "properties": { "freshness": { "const": "current" } },
            "required": ["freshness"]
          },
          "basis": { "type": "null" }
        },
        "allOf": [
          {
            "if": {
              "properties": { "consequential": { "const": true } },
              "required": ["consequential"]
            },
            "then": {
              "properties": { "result": { "const": "insufficient_context" } }
            },
            "else": {
              "properties": { "result": { "const": "deny" } }
            }
          }
        ]
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "explicit_deny" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "grants": {
            "contains": {
              "properties": { "effect": { "const": "deny" } },
              "required": ["effect"]
            }
          },
          "basis": {
            "allOf": [
              { "$ref": "#/$defs/basis" },
              {
                "properties": { "effect": { "const": "deny" } },
                "required": ["effect"]
              }
            ]
          },
          "result": { "const": "deny" }
        }
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "hard_deny" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "hard_deny": { "const": true },
          "basis": { "type": "null" },
          "result": { "const": "deny" }
        }
      }
    },
    {
      "if": {
        "properties": {
          "principal": {
            "properties": { "freshness": { "const": "current" } },
            "required": ["freshness"]
          },
          "hard_deny": { "const": false },
          "source_acl_ceiling": {
            "properties": {
              "current_authorization": {
                "properties": {
                  "connector_available": { "const": false }
                },
                "required": ["connector_available"],
                "not": { "required": ["invalidated_at"] }
              }
            },
            "required": ["current_authorization"]
          },
          "reason": { "not": { "const": "evaluation_time_invalid" } }
        },
        "required": ["principal", "hard_deny", "source_acl_ceiling"]
      },
      "then": {
        "properties": {
          "reason": {
            "enum": ["source_acl_unavailable", "source_acl_stale"]
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "principal": {
            "properties": { "freshness": { "const": "current" } },
            "required": ["freshness"]
          },
          "hard_deny": { "const": false },
          "source_acl_ceiling": {
            "properties": {
              "required": { "const": true },
              "result": { "const": "insufficient_context" }
            },
            "required": ["required", "result"]
          },
          "reason": { "not": { "const": "evaluation_time_invalid" } }
        },
        "required": ["principal", "hard_deny", "source_acl_ceiling"]
      },
      "then": {
        "properties": { "reason": { "const": "source_acl_unavailable" } }
      }
    },
    {
      "if": {
        "properties": {
          "principal": {
            "properties": { "freshness": { "const": "current" } },
            "required": ["freshness"]
          },
          "hard_deny": { "const": false },
          "source_acl_ceiling": {
            "properties": {
              "current_authorization": {
                "required": ["invalidated_at"]
              }
            },
            "required": ["current_authorization"]
          },
          "reason": { "not": { "const": "evaluation_time_invalid" } }
        },
        "required": ["principal", "hard_deny", "source_acl_ceiling"]
      },
      "then": {
        "properties": { "reason": { "const": "source_acl_invalidated" } }
      }
    },
    {
      "if": {
        "properties": {
          "principal": {
            "properties": { "freshness": { "const": "current" } },
            "required": ["freshness"]
          },
          "hard_deny": { "const": false },
          "source_acl_ceiling": {
            "properties": {
              "result": { "const": "deny" },
              "current_authorization": {
                "properties": {
                  "connector_available": { "const": true }
                },
                "required": ["connector_available"],
                "not": { "required": ["invalidated_at"] }
              }
            },
            "required": ["result", "current_authorization"]
          },
          "reason": { "not": { "const": "evaluation_time_invalid" } }
        },
        "required": ["principal", "hard_deny", "source_acl_ceiling"]
      },
      "then": {
        "properties": {
          "reason": { "enum": ["source_acl_denied", "source_acl_stale"] }
        }
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "source_acl_denied" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "source_acl_ceiling": {
            "properties": {
              "result": { "const": "deny" },
              "current_authorization": {
                "properties": {
                  "connector_available": { "const": true }
                },
                "required": ["connector_available"],
                "not": { "required": ["invalidated_at"] }
              }
            },
            "required": ["result", "current_authorization"]
          },
          "basis": { "type": "null" },
          "result": { "const": "deny" }
        }
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "source_acl_stale" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "source_acl_ceiling": {
            "properties": {
              "result": { "enum": ["allow", "deny"] },
              "current_authorization": {
                "not": { "required": ["invalidated_at"] }
              }
            },
            "required": ["result", "current_authorization", "stale_cause"]
          },
          "basis": { "type": "null" },
          "result": { "const": "deny" }
        }
      },
      "else": {
        "properties": {
          "source_acl_ceiling": {
            "not": { "required": ["stale_cause"] }
          }
        }
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "source_acl_invalidated" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "source_acl_ceiling": {
            "properties": {
              "result": { "enum": ["allow", "deny"] },
              "current_authorization": {
                "required": ["invalidated_at"]
              }
            },
            "required": ["result", "current_authorization"]
          },
          "basis": { "type": "null" },
          "result": { "const": "deny" }
        }
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "source_acl_unavailable" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "source_acl_ceiling": {
            "anyOf": [
              {
                "properties": {
                  "result": { "const": "insufficient_context" }
                },
                "required": ["result"]
              },
              {
                "properties": {
                  "result": { "enum": ["allow", "deny"] },
                  "current_authorization": {
                    "properties": {
                      "connector_available": { "const": false }
                    },
                    "required": ["connector_available"],
                    "not": { "required": ["invalidated_at"] }
                  }
                },
                "required": ["result", "current_authorization"]
              }
            ]
          },
          "basis": { "type": "null" }
        },
        "allOf": [
          {
            "if": {
              "properties": { "consequential": { "const": true } },
              "required": ["consequential"]
            },
            "then": {
              "properties": { "result": { "const": "insufficient_context" } }
            },
            "else": {
              "properties": { "result": { "const": "deny" } }
            }
          }
        ]
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "visibility_denied" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "visibility": { "const": "deny" },
          "basis": { "$ref": "#/$defs/allowBasis" },
          "result": { "const": "deny" }
        }
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "visibility_unavailable" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "visibility": { "const": "insufficient_context" },
          "basis": { "$ref": "#/$defs/allowBasis" }
        },
        "allOf": [
          {
            "if": {
              "properties": { "consequential": { "const": true } },
              "required": ["consequential"]
            },
            "then": {
              "properties": { "result": { "const": "insufficient_context" } }
            },
            "else": {
              "properties": { "result": { "const": "deny" } }
            }
          }
        ]
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "action_policy_denied" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "action_policy": { "const": "deny" },
          "basis": { "$ref": "#/$defs/allowBasis" },
          "result": { "const": "deny" }
        }
      }
    },
    {
      "if": {
        "properties": { "reason": { "const": "action_policy_unavailable" } },
        "required": ["reason"]
      },
      "then": {
        "properties": {
          "action_policy": { "const": "insufficient_context" },
          "basis": { "$ref": "#/$defs/allowBasis" }
        },
        "allOf": [
          {
            "if": {
              "properties": { "consequential": { "const": true } },
              "required": ["consequential"]
            },
            "then": {
              "properties": { "result": { "const": "insufficient_context" } }
            },
            "else": {
              "properties": { "result": { "const": "deny" } }
            }
          }
        ]
      }
    },
    {
      "if": {
        "properties": {
          "principal": { "required": ["identity_session_id"] }
        },
        "required": ["principal"]
      },
      "then": {
        "properties": {
          "principal": {
            "properties": { "type": { "const": "human" } },
            "required": ["type"]
          }
        }
      }
    },
    {
      "if": {
        "anyOf": [
          {
            "properties": {
              "grants": {
                "contains": {
                  "properties": {
                    "group": {
                      "properties": {
                        "source_kind": { "const": "oidc_group" }
                      },
                      "required": ["source_kind"]
                    }
                  },
                  "required": ["group"]
                }
              }
            },
            "required": ["grants"]
          },
          {
            "properties": {
              "basis": {
                "type": "object",
                "properties": {
                  "group": {
                    "properties": {
                      "source_kind": { "const": "oidc_group" }
                    },
                    "required": ["source_kind"]
                  }
                },
                "required": ["group"]
              }
            },
            "required": ["basis"]
          }
        ]
      },
      "then": {
        "properties": {
          "principal": {
            "required": ["identity_session_id"]
          }
        }
      }
    },
    {
      "if": {
        "properties": {
          "basis": { "type": "object", "required": ["group"] }
        },
        "required": ["basis"]
      },
      "then": {
        "properties": {
          "grants": { "contains": { "required": ["group"] } }
        }
      }
    }
  ],
  "$defs": {
    "nonEmpty": {
      "description": "Single-line opaque identifier or version string.",
      "type": "string",
      "pattern": "^\\S(?:[^\\r\\n\\u2028\\u2029]*\\S)?$"
    },
    "nonEmptyText": {
      "description": "Non-blank free-form text; may span lines.",
      "type": "string",
      "pattern": "^\\S(?:[\\s\\S]*\\S)?$"
    },
    "rfc3339Timestamp": {
      "type": "string",
      "pattern": "^\\d{4}-(?:0[1-9]|1[0-2])-(?:0[1-9]|[12]\\d|3[01])[Tt](?:[01]\\d|2[0-3]):[0-5]\\d:(?:[0-5]\\d|60)(?:\\.\\d+)?(?:[Zz]|[+-](?:[01]\\d|2[0-3]):[0-5]\\d)$"
    },
    "allowBasis": {
      "allOf": [
        { "$ref": "#/$defs/basis" },
        {
          "properties": { "effect": { "const": "allow" } },
          "required": ["effect"]
        }
      ]
    },
    "permission": {
      "type": "string",
      "enum": [
        "audit.export",
        "audit.read",
        "connector.configure",
        "connector.create",
        "connector.delete",
        "connector.read",
        "knowledge.declassify",
        "knowledge.propose",
        "knowledge.read",
        "migration.approve",
        "migration.execute",
        "obligation.read",
        "obligation.satisfy",
        "obligation.waive",
        "policy.manage",
        "policy.read",
        "proposal.approve",
        "proposal.edit",
        "proposal.read",
        "proposal.reject",
        "proposal.review",
        "semantic_executor.configure",
        "semantic_executor.qualify",
        "semantic_executor.read",
        "source.manage",
        "source.read",
        "source.sync",
        "workspace.configure",
        "workspace.manage_members",
        "workspace.read"
      ]
    },
    "decision": {
      "enum": ["allow", "deny", "insufficient_context"]
    },
    "policyInput": {
      "enum": ["allow", "deny", "insufficient_context", "not_applicable"]
    },
    "principal": {
      "type": "object",
      "required": ["id", "type", "freshness"],
      "additionalProperties": false,
      "properties": {
        "id": { "$ref": "#/$defs/nonEmpty" },
        "identity_session_id": {
          "description": "Exact E2.3 human identity session used at evaluation. The oidc_group source kind is claim-only in V1 and valid only for a human principal; nonhuman principals must omit this field. Required for any oidc_group grant or winning basis; it must equal the identity session retained by source_event_id, and replay fails closed on mismatch.",
          "$ref": "#/$defs/nonEmpty"
        },
        "type": { "enum": ["human", "service", "agent", "workload"] },
        "freshness": { "enum": ["current", "expired", "insufficient_context"] }
      }
    },
    "sourceResource": {
      "description": "Repository, project, space, or channel level within a scope. id is the bare source-local resource identifier; parent container context is carried separately by source_container_id — required on a decision resource with source_acl_ceiling.required true and on current ACL evidence beside it, optional on a decision resource with no required check and on grant scopes.",
      "type": "object",
      "required": ["kind", "id"],
      "additionalProperties": false,
      "properties": {
        "kind": { "enum": ["repository", "project", "space", "channel"] },
        "id": { "$ref": "#/$defs/nonEmpty" }
      }
    },
    "scope": {
      "type": "object",
      "required": ["workspace_id"],
      "additionalProperties": false,
      "properties": {
        "workspace_id": { "$ref": "#/$defs/nonEmpty" },
        "connector_id": { "$ref": "#/$defs/nonEmpty" },
        "source_container_id": {
          "description": "Provider-owned parent container identifier for a bare source resource id. When the provider exposes no intermediate container, this is the provider-owned tenant or account identifier for the connector.",
          "$ref": "#/$defs/nonEmpty"
        },
        "resource": { "$ref": "#/$defs/sourceResource" },
        "knowledge_kind": { "$ref": "#/$defs/nonEmpty" },
        "object_id": { "$ref": "#/$defs/nonEmpty" }
      }
    },
    "role": {
      "type": "object",
      "required": ["id", "version"],
      "additionalProperties": false,
      "properties": {
        "id": { "$ref": "#/$defs/nonEmpty" },
        "version": { "type": "integer", "minimum": 1 }
      }
    },
    "membershipObservation": {
      "description": "Exact immutable, non-nested external membership observation selected by the evaluator. A group grant constrains member_present to true; retained absence evidence constrains it to false. Latest-observation selection is evaluator-owned and fails closed.",
      "type": "object",
      "required": [
        "id",
        "external_identity_link_id",
        "source_event_id",
        "observed_at",
        "effective_at",
        "fresh_until",
        "member_present",
        "nested"
      ],
      "additionalProperties": false,
      "properties": {
        "id": {
          "description": "Stable identifier of the retained membership observation. That record identifies one exact external binding and one exact identity link. Replay verifies that the observation's retained binding equals the enclosing binding_id, that the retained binding's AgentDoc group equals the enclosing group id, and that the observation's retained identity link equals the enclosing identity-link identifier, so the observation's subject is the authorization envelope principal; any mismatch fails closed.",
          "$ref": "#/$defs/nonEmpty"
        },
        "external_identity_link_id": {
          "description": "Verified E2.3 identity link through which the external source identity resolves to this workspace principal. Its preserved lifecycle retains the link and unlink instants. Evaluation and replay fail closed unless the link belongs to the authorization envelope principal and was continuously active from observed_at through the decision evaluation time; an unlink after an earlier decision preserves that decision's replay but revokes future use, including after a subsequent relink.",
          "$ref": "#/$defs/nonEmpty"
        },
        "source_event_id": {
          "description": "Immutable provenance record retained so replay can re-derive the observation from source. Replay verifies that the source record belongs to the observation's retained binding and that its event or per-principal run membership subject resolves through the observation's retained external identity link; any mismatch fails closed. For an upstream connector event, it identifies the event that prompted a current-state membership read and carries its current-state read and ingestion-commit instants; the ingestion transaction commits only after the successful read. For a synchronization run, it identifies the run and carries its start and completion instants plus the retained per-principal read result; a resynchronization sweep records one identifier for the run and cites it from every observation it produces. For claim-only oidc_group, it identifies the exact authenticated identity-session event and carries its token issuance, validation/ingestion-commit, and session-expiry instants.",
          "$ref": "#/$defs/nonEmpty"
        },
        "observed_at": {
          "description": "Source evidence time used for membership freshness. An event-driven positive observation carries the current-state source read time prompted by its event, never the webhook delivery time; a delayed or reordered positive event that the current-state read no longer confirms produces no positive observation. A resynchronization sweep carries the per-principal read time when available, otherwise the sweep start as a conservative lower bound; it never substitutes the later run-completion time, so a long sweep cannot extend stale membership. For claim-only oidc_group, observed_at is the token issuance instant retained by source_event_id; validation of that freshly issued token at authentication is the current-state read.",
          "$ref": "#/$defs/rfc3339Timestamp"
        },
        "effective_at": {
          "description": "Instant this observation became eligible to confer grants. It must equal the ingestion-commit instant of the connector event, the completion instant of the synchronization run, or, for claim-only oidc_group, the authentication validation/ingestion-commit instant identified by source_event_id. For a sweep completing a binding-mode reconfiguration this equals the new epoch boundary; a routine sweep records into the epoch already in effect. For claim-only oidc_group, principal.identity_session_id must equal the identity session retained by source_event_id; the observation may confer only during that matching session, no later than the cited identity session's expiry. observed_at must not follow effective_at, and effective_at must not follow evaluation_time; comparisons are evaluator-owned and fail closed.",
          "$ref": "#/$defs/rfc3339Timestamp"
        },
        "fresh_until": {
          "description": "Immutable deadline derived from observed_at under the versioned freshness policy retained by the exact binding. Replay resolves the policy version in effect at observed_at, recomputes the deadline, requires fresh_until to equal that recomputation, caps oidc_group at the cited identity session's expiry, and fails closed unless evaluation_time does not exceed fresh_until; connector unavailability cannot extend it. observed_at must not follow fresh_until, and fresh_until must follow effective_at, so an observation is never expired before it becomes eligible. A synchronization run whose produced observations cannot satisfy that ordering is rejected as a freshness-policy misconfiguration; a routine replacement run must also complete before the deadline of the observations it replaces. Timestamp comparisons are evaluator-owned and fail closed.",
          "$ref": "#/$defs/rfc3339Timestamp"
        },
        "member_present": { "type": "boolean" },
        "nested": { "const": false }
      }
    },
    "membershipAbsenceEvidence": {
      "description": "Immutable confirmed-absence provenance for one relevant group membership input.",
      "oneOf": [
        {
          "description": "Manual absence replays by resolving the stable group and scanning the retained complete manual-membership lifecycle history for the authorization envelope principal at evaluation_time; any active lifecycle record makes this absence invalid and fails closed.",
          "type": "object",
          "required": ["group_id", "group_name", "membership_source"],
          "additionalProperties": false,
          "properties": {
            "group_id": { "$ref": "#/$defs/nonEmpty" },
            "group_name": {
              "description": "AgentDoc group name in effect at evaluation_time, verified against the retained complete group-name history whose versions each record an effective instant.",
              "$ref": "#/$defs/nonEmpty"
            },
            "membership_source": { "const": "manual" }
          }
        },
        {
          "description": "External absence replays against the exact retained binding, mode epoch, identity link, and negative observation. Replay applies the same binding/group/source-kind, identity-link lifecycle, source-record ownership and subject, timing, and freshness checks as positive external group provenance; any mismatch fails closed.",
          "type": "object",
          "required": [
            "group_id",
            "group_name",
            "membership_source",
            "binding_id",
            "binding_mode",
            "source_kind",
            "binding_mode_effective_at",
            "membership_observation"
          ],
          "additionalProperties": false,
          "properties": {
            "group_id": { "$ref": "#/$defs/nonEmpty" },
            "group_name": {
              "description": "AgentDoc group name in effect at evaluation_time, verified against the retained complete group-name history whose versions each record an effective instant.",
              "$ref": "#/$defs/nonEmpty"
            },
            "membership_source": { "const": "external" },
            "binding_id": { "$ref": "#/$defs/nonEmpty" },
            "binding_mode": {
              "enum": ["authoritative_sync", "additive_sync"]
            },
            "source_kind": {
              "enum": [
                "github_team",
                "gitlab_group",
                "slack_user_group",
                "oidc_group",
                "scim_group"
              ]
            },
            "binding_mode_effective_at": {
              "$ref": "#/$defs/rfc3339Timestamp"
            },
            "membership_observation": {
              "allOf": [
                { "$ref": "#/$defs/membershipObservation" },
                {
                  "properties": { "member_present": { "const": false } },
                  "required": ["member_present"]
                }
              ]
            }
          }
        }
      ]
    },
    "group": {
      "description": "AgentDoc-owned group provenance with the immutable membership fact used at evaluation. When manual and external membership both confer the same group grant, the evaluator records the independently durable manual membership. External membership may confer a grant only through an authoritative or additive binding; suggestion-only and disabled bindings never appear in an authorization basis.",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "id",
            "name",
            "membership_source",
            "membership_id",
            "membership_created_at"
          ],
          "additionalProperties": false,
          "properties": {
            "id": { "$ref": "#/$defs/nonEmpty" },
            "name": {
              "description": "AgentDoc group name in effect at evaluation_time. Replay verifies id and name against the retained complete effective group-name history, whose versions each record an effective instant; any mismatch fails closed while later renames preserve historical replay.",
              "$ref": "#/$defs/nonEmpty"
            },
            "membership_source": { "const": "manual" },
            "membership_id": {
              "description": "Stable identifier of the preserved manual-membership lifecycle record that conferred this grant. Replay verifies that it belongs to the authorization envelope principal and enclosing group id, was created by evaluation_time, and was not then revoked; any mismatch fails closed.",
              "$ref": "#/$defs/nonEmpty"
            },
            "membership_created_at": {
              "description": "Creation time of the manual membership identified by membership_id, exactly matching that record's creation time. It must not follow evaluation_time; both comparisons are evaluator-owned.",
              "$ref": "#/$defs/rfc3339Timestamp"
            }
          }
        },
        {
          "description": "binding_mode and binding_mode_effective_at name the binding's mode epoch in effect at evaluation_time; membership_observation.effective_at must fall within that epoch and not follow evaluation_time. For a connector event or synchronization run, membership_observation.observed_at and effective_at must resolve against the same record identified by membership_observation.source_event_id: observed_at equals that event's current-state read instant or falls within that run, while effective_at equals that event's ingestion-commit instant or that run's completion instant. For claim-only oidc_group, the same identifier instead retains the exact identity session and its token-issuance, validation/ingestion-commit, and expiry instants. Only the transition sweep that opened the epoch — the observation whose effective_at equals binding_mode_effective_at — may carry an observed_at preceding the epoch, and never one preceding that run's start; every other observation's observed_at must fall at or after binding_mode_effective_at. Those instant comparisons are evaluator-owned and fail closed.",
          "type": "object",
          "required": [
            "id",
            "name",
            "membership_source",
            "binding_id",
            "binding_mode",
            "source_kind",
            "binding_mode_effective_at",
            "membership_observation"
          ],
          "additionalProperties": false,
          "properties": {
            "id": { "$ref": "#/$defs/nonEmpty" },
            "name": {
              "description": "AgentDoc group name in effect at evaluation_time. Replay verifies id and name against the retained complete effective group-name history, whose versions each record an effective instant; any mismatch fails closed while later renames preserve historical replay.",
              "$ref": "#/$defs/nonEmpty"
            },
            "membership_source": { "const": "external" },
            "binding_id": {
              "description": "Stable identifier of the preserved external-binding record and its complete effective mode history. Replay verifies binding_mode and binding_mode_effective_at against the retained epoch.",
              "$ref": "#/$defs/nonEmpty"
            },
            "binding_mode": {
              "enum": ["authoritative_sync", "additive_sync"]
            },
            "binding_mode_effective_at": {
              "description": "Start of the binding-mode epoch in effect at evaluation_time. membership_observation.effective_at must fall within that same epoch before the observation can confer this grant.",
              "$ref": "#/$defs/rfc3339Timestamp"
            },
            "membership_observation": {
              "allOf": [
                { "$ref": "#/$defs/membershipObservation" },
                {
                  "properties": { "member_present": { "const": true } },
                  "required": ["member_present"]
                }
              ]
            },
            "source_kind": {
              "description": "Closed source adapter kind for this membership. Replay resolves binding_id and fails closed unless this value equals the source kind recorded on the retained binding.",
              "enum": [
                "github_team",
                "gitlab_group",
                "slack_user_group",
                "oidc_group",
                "scim_group"
              ]
            }
          }
        }
      ]
    },
    "grant": {
      "type": "object",
      "required": ["grant_id", "source", "effect", "permission", "scope"],
      "additionalProperties": false,
      "properties": {
        "grant_id": { "$ref": "#/$defs/nonEmpty" },
        "source": { "enum": ["role_assignment", "direct_grant"] },
        "effect": { "enum": ["allow", "deny"] },
        "permission": { "$ref": "#/$defs/permission" },
        "scope": { "$ref": "#/$defs/scope" },
        "expires_at": {
          "description": "Optional expiry for a role assignment and required expiry for every direct grant, regardless of effect. The schema pins the lexical RFC 3339 shape; calendar validity and instant comparison are evaluator-owned and fail closed.",
          "$ref": "#/$defs/rfc3339Timestamp"
        },
        "exceptional_reason": {
          "description": "Required for every human direct grant, regardless of effect.",
          "$ref": "#/$defs/nonEmptyText"
        },
        "role": { "$ref": "#/$defs/role" },
        "group": {
          "description": "Present only when this grant is conferred through the recorded AgentDoc group; absence means the grant is principal-attributed.",
          "$ref": "#/$defs/group"
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "source": { "const": "direct_grant" } },
            "required": ["source"]
          },
          "then": {
            "required": ["expires_at"],
            "not": { "required": ["role"] }
          },
          "else": {
            "required": ["role"],
            "not": { "required": ["exceptional_reason"] }
          }
        }
      ]
    },
    "sourceAclCheckContext": {
      "description": "Immutable inline context for an attempted required source ACL check. After the earlier identity, evaluation-time, and hard-deny gates pass, it is required when no current_authorization exists and carries the only retained provider-identity and policy bindings for that evidence-free attempt: external_identity_link_id must name the effective decision principal's retained provider identity link resolving that principal at evaluation_time, and acl_policy_version must name the connector policy governing the attempt. Beside definitive current_authorization it gives consumers a uniform read path for those bindings: it restates the evidence version while policy is unchanged. Whenever policy changed, the evaluator retains this context with the different version governing at evaluation_time, including an expired record where stale_cause expired wins, so replay distinguishes supersession from an unchanged policy. stale_cause policy_superseded structurally requires this context; the combined expired case is an evaluator-owned obligation because JSON Schema cannot detect an arbitrary version change. Resource and principal bindings come from the enclosing decision rather than being duplicated here. For delegated execution, the workload or service principal uses its own linked provider identity while the delegator remains in the separate delegation chain. These bindings are evaluator-owned and fail closed.",
      "type": "object",
      "required": [
        "role",
        "external_identity_link_id",
        "acl_policy_version"
      ],
      "additionalProperties": false,
      "properties": {
        "role": { "const": "attempted_check" },
        "external_identity_link_id": { "$ref": "#/$defs/nonEmpty" },
        "acl_policy_version": {
          "description": "Connector ACL policy version governing this attempt. With the enclosing decision's resource.connector_id, which resolves the configured connector instance and kind, it identifies the retained adoc.connector_acl_policy.v0 record whose policy_version equals this value.",
          "$ref": "#/$defs/nonEmpty"
        }
      }
    },
    "sourceAclCeiling": {
      "description": "Source ACL gate input. Snapshot provenance is defined by source_acl_ceiling.snapshot_id; each trigger for the decision resource-scope rule is stated on the field that carries it. Once the identity, evaluation-time, and hard-deny gates pass, check_context is structurally required when no current_authorization exists or when stale_cause is policy_superseded; definitive unchanged-policy evidence may stand alone. Evaluator-owned policy-change retention, including the expiry-wins case JSON Schema cannot detect, is defined by sourceAclCheckContext. identity_context_missing, identity_expired, evaluation_time_invalid, or hard_deny may win before an attempt and omit context. required true with an allow or deny result additionally requires current ACL evidence.",
      "type": "object",
      "required": ["required", "result"],
      "additionalProperties": false,
      "properties": {
        "required": {
          "description": "Whether the decision resource required a source ACL check, independent of whether evaluation reached that gate. True requires connector_id, source_container_id, and resource on the decision resource and forces result to allow, deny, or insufficient_context. False forces not_applicable and forbids check_context, current_authorization, and stale_cause.",
          "type": "boolean"
        },
        "result": {
          "description": "Provider ACL verdict when one was observed, including evidence later found stale, invalidated, or unavailable; insufficient_context means no definitive provider verdict was available, while not_applicable means no source ACL check was required. The decision reason records freshness and availability precedence.",
          "$ref": "#/$defs/policyInput"
        },
        "check_context": {
          "description": "Provider-identity and evaluation-time governing-policy bindings for an attempted required source ACL check. Field bindings and policy-change retention are defined by sourceAclCheckContext.",
          "$ref": "#/$defs/sourceAclCheckContext"
        },
        "snapshot_id": {
          "description": "Immutable historical ACL snapshot consulted by this decision for audit provenance. Its presence requires connector_id, source_container_id, and resource on the enclosing decision resource even when required is false, so the snapshot remains bound to the source scope being authorized. current_authorization repeats the snapshot bound to its independently stored evidence record so the evaluator can prove freshness applies to this exact consulted payload. Both snapshot_id values must match; equality is evaluator-owned and fails closed. The snapshot never establishes current authorization by itself.",
          "$ref": "#/$defs/nonEmpty"
        },
        "current_authorization": {
          "description": "Freshness-bounded evidence for a required definitive source ACL result; field bindings are defined by currentSourceAclAuthorization.",
          "$ref": "#/$defs/currentSourceAclAuthorization"
        },
        "stale_cause": {
          "description": "Recorded cause required exactly when reason is source_acl_stale. expired means evaluation_time exceeded current_authorization.expires_at and wins when both expiry and policy change are observed; when policy also changed, the evaluator retains check_context naming the different evaluation-time governing version so replay distinguishes that record from unchanged-policy expiry. policy_superseded means the evidence is unexpired, current_authorization.acl_policy_version governed at observed_at, and required check_context.acl_policy_version records a different policy version governing at evaluation_time. Timestamp comparison and cross-field version inequality are evaluator-owned and fail closed.",
          "enum": ["expired", "policy_superseded"]
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "required": { "const": false } },
            "required": ["required"]
          },
          "then": {
            "properties": { "result": { "const": "not_applicable" } },
            "not": {
              "anyOf": [
                { "required": ["check_context"] },
                { "required": ["current_authorization"] },
                { "required": ["stale_cause"] }
              ]
            }
          },
          "else": {
            "properties": {
              "result": { "enum": ["allow", "deny", "insufficient_context"] }
            }
          }
        },
        {
          "if": { "required": ["stale_cause"] },
          "then": { "required": ["current_authorization"] }
        },
        {
          "if": {
            "properties": { "stale_cause": { "const": "policy_superseded" } },
            "required": ["stale_cause"]
          },
          "then": { "required": ["check_context", "current_authorization"] }
        },
        {
          "if": {
            "properties": { "result": { "enum": ["allow", "deny"] } },
            "required": ["result"]
          },
          "then": { "required": ["snapshot_id"] }
        },
        {
          "if": {
            "properties": {
              "required": { "const": true },
              "result": { "enum": ["allow", "deny"] }
            },
            "required": ["required", "result"]
          },
          "then": { "required": ["current_authorization"] }
        },
        {
          "if": {
            "properties": {
              "result": {
                "not": { "enum": ["allow", "deny"] }
              }
            },
            "required": ["result"]
          },
          "then": {
            "not": { "required": ["current_authorization"] }
          }
        },
        {
          "if": {
            "properties": { "result": { "const": "allow" } },
            "required": ["result", "current_authorization"]
          },
          "then": {
            "properties": {
              "current_authorization": {
                "properties": { "result": { "const": "allow" } }
              }
            }
          }
        },
        {
          "if": {
            "properties": { "result": { "const": "deny" } },
            "required": ["result", "current_authorization"]
          },
          "then": {
            "properties": {
              "current_authorization": {
                "properties": { "result": { "const": "deny" } }
              }
            }
          }
        }
      ]
    },
    "currentSourceAclAuthorization": {
      "description": "Freshness-bounded current authorization input. Historical source ACL snapshots remain provenance only. workspace_id, connector_id, source_container_id, and source must match the decision resource; principal_id must match the effective decision principal; external_identity_link_id must match check_context when it is present. observed_at must equal the referenced snapshot's observed_at, and expires_at is derived from that immutable snapshot observation instant under the retained connector ACL policy, so a later authorization record cannot refresh historical ACL data. Policy-version comparison and retention, including combined expiry and supersession, are defined by sourceAclCheckContext and source_acl_ceiling.stale_cause. The retained identity link independently binds this evidence to principal_id and must resolve it continuously from observed_at through evaluation_time so evidence observed before linking or after revocation can never widen access; snapshot_id and result must match their sibling source_acl_ceiling values. An earlier identity, evaluation-time, or hard-deny outcome may retain this downstream input without check_context for replay, but it can never override that earlier outcome. Result equality is schema-enforced; the remaining equality and temporal checks are evaluator-owned and fail closed.",
      "type": "object",
      "required": [
        "role",
        "current_acl_id",
        "result",
        "snapshot_id",
        "workspace_id",
        "connector_id",
        "source_container_id",
        "principal_id",
        "external_identity_link_id",
        "source",
        "acl_policy_version",
        "observed_at",
        "expires_at",
        "connector_available"
      ],
      "additionalProperties": false,
      "properties": {
        "role": { "const": "current_authorization" },
        "current_acl_id": {
          "description": "Immutable identifier of this freshness-bounded current ACL authorization record.",
          "$ref": "#/$defs/nonEmpty"
        },
        "result": {
          "description": "Provider verdict bound to this evidence record. It is a source-content readability and visibility ceiling shared across AgentDoc permissions, not a claim of provider write or administrative authority; the AgentDoc grant supplies operation authority. It must match source_acl_ceiling.result; the schema enforces this equality, so an evaluator that observes the independently stored evidence disagreeing with the decision ceiling fails closed and records source_acl_unavailable rather than emitting the mismatch.",
          "enum": ["allow", "deny"]
        },
        "snapshot_id": {
          "description": "Retained adoc.source_acl_snapshot.v0 record establishing which ACL payload was evaluated, bound to this independently stored current-evidence record. Repeating it beside source_acl_ceiling.snapshot_id lets the evaluator reject evidence issued for a different payload; both values must match, and that equality is evaluator-owned and fails closed. Currency is established by expires_at, connector_available, and absence of invalidated_at, not by the snapshot alone.",
          "$ref": "#/$defs/nonEmpty"
        },
        "workspace_id": {
          "description": "Workspace in which this ACL evidence was observed. It must match resource.workspace_id; equality is evaluator-owned and fails closed.",
          "$ref": "#/$defs/nonEmpty"
        },
        "connector_id": { "$ref": "#/$defs/nonEmpty" },
        "source_container_id": {
          "description": "Provider-owned parent container for the bare source id, or the provider-owned tenant or account identifier when no intermediate container exists. It must match both resource.source_container_id and the referenced snapshot; equality is evaluator-owned and fails closed.",
          "$ref": "#/$defs/nonEmpty"
        },
        "principal_id": { "$ref": "#/$defs/nonEmpty" },
        "external_identity_link_id": {
          "description": "Immutable retained identity-link record for the exact provider identity whose ACL was evaluated. It must resolve principal_id continuously from observed_at through evaluation_time; equality and temporal validity are evaluator-owned and fail closed.",
          "$ref": "#/$defs/nonEmpty"
        },
        "source": { "$ref": "#/$defs/sourceResource" },
        "acl_policy_version": {
          "description": "Connector ACL policy version in force when this current evidence was observed. Together with connector_id, which resolves the configured connector instance and kind, it identifies the retained adoc.connector_acl_policy.v0 record whose policy_version equals this value. Evidence is stale when evaluation_time exceeds expires_at or when a different policy version governs at evaluation_time. A policy-version change invalidates even unexpired evidence and requires refresh. Observation-time/evaluation-time version binding and expiry-wins retention are defined by sourceAclCheckContext and source_acl_ceiling.stale_cause. This value must also match acl_policy_version on the corresponding adoc.source_acl_snapshot.v0 record. Equality, inequality, and temporal checks are evaluator-owned and fail closed.",
          "$ref": "#/$defs/nonEmpty"
        },
        "observed_at": {
          "description": "Immutable observation instant repeated from the referenced snapshot. It must equal that snapshot's observed_at; equality and instant comparison are evaluator-owned and fail closed. The schema pins the lexical RFC 3339 shape only.",
          "$ref": "#/$defs/rfc3339Timestamp"
        },
        "expires_at": {
          "description": "Freshness bound derived from the referenced snapshot observation instant using freshness_window_seconds from the retained adoc.connector_acl_policy.v0 record identified by connector_id and acl_policy_version. Evidence is stale when evaluation_time exceeds expires_at or when the policy version governing at evaluation_time differs from current_authorization.acl_policy_version. Stale-cause precedence and governing-policy retention are defined by source_acl_ceiling.stale_cause and sourceAclCheckContext. Once observed, either stale cause remains definitive when the connector later becomes unavailable, so the evaluator records source_acl_stale rather than source_acl_unavailable. JSON Schema cannot compare instants or arbitrary version strings; replay enforces the ordering and inequality fail closed.",
          "$ref": "#/$defs/rfc3339Timestamp"
        },
        "connector_available": {
          "description": "Whether the connector was available when this evidence was evaluated. False forbids authorization from widening access. Unless identity_context_missing, identity_expired, evaluation_time_invalid, or hard_deny already won, false without invalidated_at requires reason source_acl_unavailable or source_acl_stale; a known invalidation remains source_acl_invalidated. Conversely, source_acl_unavailable over an allow or deny ceiling requires false and no invalidated_at.",
          "type": "boolean"
        },
        "invalidated_at": {
          "description": "Set when a revocation observation invalidated this evidence. Unless identity_context_missing, identity_expired, evaluation_time_invalid, or hard_deny already won, presence requires reason source_acl_invalidated; conversely that reason requires presence. Presence forbids a top-level allow; source_acl_ceiling.result may still preserve the provider verdict observed before invalidation. An observed invalidation remains definitive when the connector later becomes unavailable.",
          "$ref": "#/$defs/rfc3339Timestamp"
        }
      }
    },
    "basis": {
      "description": "Denormalized winning-grant summary emitted by the trusted evaluator. grant_id, source, effect, role, and group must exactly match the grants entry selected by grant_id; scope_match must exactly match that grant's scope. Standard JSON Schema cannot express that arbitrary array join, so consumers must replay or otherwise verify the match before trusting the basis for audit display.",
      "type": "object",
      "required": ["grant_id", "source", "effect", "scope_match"],
      "additionalProperties": false,
      "properties": {
        "grant_id": { "$ref": "#/$defs/nonEmpty" },
        "source": { "enum": ["role_assignment", "direct_grant"] },
        "effect": { "enum": ["allow", "deny"] },
        "scope_match": { "$ref": "#/$defs/scope" },
        "role": { "$ref": "#/$defs/role" },
        "group": {
          "description": "Evaluator-owned exact copy of the winning grant group when that grant is group-conferred. The schema additionally requires at least one group-bearing grant when this field is present; replay verifies that the selected grant is that grant and that omission means the winner is principal-attributed.",
          "$ref": "#/$defs/group"
        }
      },
      "allOf": [
        {
          "if": {
            "properties": { "source": { "const": "direct_grant" } },
            "required": ["source"]
          },
          "then": { "not": { "required": ["role"] } },
          "else": { "required": ["role"] }
        }
      ]
    }
  }
}
