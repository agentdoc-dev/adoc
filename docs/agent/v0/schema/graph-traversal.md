# AgentDoc Graph Traversal Schema

The V2.2 graph traversal surface is `adoc.graph.traversal.v0`.

Graph traversal returns a root Object ID, related Knowledge Object nodes, relation edges, and diagnostics. It preserves original edge direction even when the traversal direction is incoming or both.

Use graph traversal to explain dependency, supersession, and related-object context.
