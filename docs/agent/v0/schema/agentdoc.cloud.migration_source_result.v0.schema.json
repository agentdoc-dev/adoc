{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "adoc://agent/v0/schema/agentdoc.cloud.migration_source_result.v0.schema.json",
  "title": "agentdoc.cloud.migration_source_result.v0",
  "description": "Shared exact-byte native transport for source target or checkpoint receipts. Verify SHA256 and all native bindings; there is no parsed authoritative duplicate. The target receipt digest is its target_digest.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "receipt_bytes_base64",
    "receipt_digest"
  ],
  "properties": {
    "schema_version": {
      "const": "agentdoc.cloud.migration_source_result.v0"
    },
    "receipt_bytes_base64": {
      "type": "string",
      "minLength": 4,
      "pattern": "^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$"
    },
    "receipt_digest": {
      "description": "SHA256 of the exact native canonical receipt JSON plus one LF; for target receipts this equals target_digest.",
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    }
  }
}
